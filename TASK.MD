# Feature Name Implementation

Brief description of the feature and its purpose.

## Completed Tasks

- [x] 2026-08-14 07:24 UTC: Security remediation batch 3 (findings 9-15, 17, 21). Public story lists/detail strip submitter email, team emails, rejection/spam fields, customMessage, and changeLog. listPending and comments.listPendingByStory require moderation.view. listUserStories is approved and not hidden only. getValidatedGroupScores/getValidatedAiResults take the real password and verify it server-side (admins still pass); sessionStorage stores the password, not "true". getGroupSubmissions, updateSubmissionStatus, and getSubmissionNotes require a judge sessionId (admin notes still use judgeId behind judging.tracking). registerJudge re-checks the group password and issues session ids from crypto.getRandomValues. hashPassword is SHA-256 hex; verifyPassword still accepts legacy btoa hashes. Verified: convex tsc exit 0, zero lints, app tsc no new errors in touched files. PRD: prds/security-review-2026-08-13.md. Files: convex/stories.ts, convex/validators.ts, convex/users.ts, convex/comments.ts, convex/judgeScores.ts, convex/aiJudge.ts, convex/judgingGroupSubmissions.ts, convex/judges.ts, convex/judgingGroups.ts, src/pages/PublicJudgingResultsPage.tsx, src/components/PublicJudgingResultsDashboard.tsx, src/pages/AIJudgeResultsPage.tsx, src/pages/JudgingGroupPage.tsx, src/pages/JudgingInterfacePage.tsx, src/components/admin/JudgeTracking.tsx, src/components/admin/judging/GroupLinksSection.tsx.

- [x] 2026-08-14 06:58 UTC: Security remediation batch 2 (findings 6-8). Stopped public profile/follow queries from returning email and clerkId. getUserProfileByUsername now projects a public user shape and returns a server-computed isOwnProfile boolean so the profile page no longer compares Clerk ids. getFollowers/getFollowing return only _id, name, username, imageUrl, isVerified. adminFollowsQueries getTopUsersByFollowers/getTopUsersByFollowing/getTotalFollowRelationships now require numbers.view and return name/username/count only. UserProfilePage and NumbersView updated to the new shapes. Verified: convex tsc exit 0, zero lints, app tsc no new errors in touched files. PRD: prds/security-review-2026-08-13.md. Files: convex/validators.ts, convex/users.ts, convex/follows.ts, convex/adminFollowsQueries.ts, src/pages/UserProfilePage.tsx, src/components/admin/NumbersView.tsx.

- [x] 2026-08-13 06:52 UTC: Security remediation batch 1 (findings 1-5 from the review). Deleted four leftover [TEMPORARY] backdoors from convex/users.ts after confirming zero frontend/backend callers: setUserAsAdminByUsername and setUserAsAdminByEmail (both patched any user to role admin with no auth), listAllUsersForDebug (unauthenticated full-table email/username/role dump), and setCurrentUserAsAdminTemp (any signed-in user self-escalating to admin). Restored the permission gate on convex/emails/broadcast.ts debugUsers and searchUsers (was commented out, leaking all emails+clerkIds to any signed-in user) with requirePermission(ctx, "emails.send") matching the file's other senders, and removed the per-user PII console.log lines; kept the functions since the admin EmailManagement recipient picker calls them. Verified: convex tsc exit 0, zero lints. PRD: prds/security-review-2026-08-13.md. Files: convex/users.ts, convex/emails/broadcast.ts.

- [x] 2026-08-13 06:35 UTC: Full-app security review (/sec-check) plus dependency cleanup. Audited all 341 public Convex functions (query/mutation/action) across three passes: auth enforcement, data exposure, and integrations. Found 22 ungated findings headlined by leftover [TEMPORARY] backdoors in convex/users.ts (setUserAsAdminByUsername/setUserAsAdminByEmail patch any user to admin with no auth; listAllUsersForDebug dumps every user's email; setCurrentUserAsAdminTemp self-escalates), commented-out admin checks in convex/emails/broadcast.ts (debugUsers/searchUsers leak all emails+clerkIds to any signed-in user), email/clerkId exposure via getUserProfileByUsername + follows.getFollowers/getFollowing + adminFollowsQueries, submitter PII + moderation fields on public story lists/detail, client-trusted password bypass in getValidatedGroupScores/getValidatedAiResults, and ungated getGroupSubmissions team-email leak. Verified OK: Clerk/Resend webhook signature checks, all schedulers/crons target internal.*, secrets server-side only, agent judging HTTP API, migrations gated. Dependency fix (code change this session): removed deprecated unused @clerk/clerk-sdk-node and bumped @auth/core to ^0.41.3, dropping npm audit from 24 vulnerabilities (2 critical, 16 high) to 2 moderate (react-router, needs a breaking v7 upgrade — no patched 6.x exists). Review only for the backend findings; no security code changed pending go-ahead. PRD: prds/security-review-2026-08-13.md. Files: package.json, package-lock.json.

- [x] 2026-08-14 05:00 UTC: Choice fields round two: modern controls, dropdown type, answer counts, judge filter. Restyled ChoiceFieldInput radio/checkbox controls to 20px custom-styled inputs (appearance-none + theme tokens, Lucide Check for checkboxes) inside full-width tappable rows with hover/checked states. Added a third choice field type `select` (dropdown) end to end: fieldType unions in convex/schema.ts, storyFormFields.ts (validator + sanitizeChoiceValue treats select like radio), judgingGroups.ts (min-2-options rule), submitForms.ts; ChoiceFieldInput renders it via the themed SimpleSelect with placeholder support; both admin editors (FormFieldManagement, GroupSubmitPageSection) offer "Dropdown (select)" in the type picker; all five submit surfaces pass placeholder through. New admin query storyFormFields.getChoiceAnswerCounts (forms.view) aggregates stories.dynamicFormValues per choice field (multiselect split on the separator, totals count stories that answered); FormFieldManagement shows a mini bar + count per option with a responses total on each choice field row. JudgingInterfacePage gained a "Filter by answer" SimpleSelect listing "Field label: option" pairs from enabled dynamic choice fields and the group's custom choice questions, filtering client-side over dynamicFormValues and customFormAnswers (multiselect parsed) and composing with the existing tag/status/search filters; Clear filters resets it. Verified: app tsc zero errors in touched files (remaining pre-existing), convex tsc clean, zero lints. PRD: prds/story-form-choice-fields.md (follow-up section). Files: convex/schema.ts, convex/storyFormFields.ts, convex/judgingGroups.ts, convex/submitForms.ts, src/components/ui/ChoiceFieldInput.tsx, src/components/StoryForm.tsx, src/components/DynamicSubmitForm.tsx, src/components/YCHackForm.tsx, src/components/ResendForm.tsx, src/pages/JudgingGroupSubmitPage.tsx, src/pages/JudgingInterfacePage.tsx, src/components/admin/FormFieldManagement.tsx, src/components/admin/judging/GroupSubmitPageSection.tsx, src/components/admin/judging/groupSection.tsx.

- [x] 2026-08-14 04:15 UTC: Radio and multi-select story form fields. Added `radio` and `multiselect` to the `fieldType` unions on storyFormFields and judgingGroups.submissionCustomQuestions with a new optional `options: string[]` on both. Backend: shared `storyFormFieldTypeValidator`/`storyFormFieldDoc` in convex/storyFormFields.ts, create/update accept options, `resolveDynamicFieldValues` sanitizes choice answers server side via `sanitizeChoiceValue` (values must match configured options; multiselect stored as a comma-joined string with `MULTISELECT_SEPARATOR` so `value` stays a plain string for judging UI, CSV export, and the AI judge); judgingGroups.update enforces at least 2 options on choice questions; submitForms query validators widened. Frontend: new reusable src/components/ui/ChoiceFieldInput.tsx (radio group / checkbox group, native required handling, aria-labelledby, multiselect preserves configured option order) rendered from StoryForm, DynamicSubmitForm, JudgingGroupSubmitPage (dynamic fields + custom questions), YCHackForm, and ResendForm. Admin: FormFieldManagement field-type picker gained Radio and Multi-select with a one-option-per-line textarea (min 2 enforced in add and edit); GroupSubmitPageSection custom questions editor gained the same types + options editor; groupSection CustomQuestion type carries options. Display: StoryDetail sidebar (desktop + mobile) falls back to story.dynamicFormValues for fields without a dedicated stories column so choice answers render as Label: value. Existing field types, judging group overrides, and permissions untouched. Verified: app tsc zero errors in touched files (remaining pre-existing elsewhere), convex dev push green, zero lints. PRD: prds/story-form-choice-fields.md. Files: convex/schema.ts, convex/storyFormFields.ts, convex/judgingGroups.ts, convex/submitForms.ts, src/components/ui/ChoiceFieldInput.tsx (new), src/components/StoryForm.tsx, src/components/DynamicSubmitForm.tsx, src/components/YCHackForm.tsx, src/components/ResendForm.tsx, src/pages/JudgingGroupSubmitPage.tsx, src/components/admin/FormFieldManagement.tsx, src/components/admin/judging/GroupSubmitPageSection.tsx, src/components/admin/judging/groupSection.tsx, src/components/StoryDetail.tsx.

- [x] 2026-08-14 01:15 UTC: In-app spam review requests (dispute loop that skips email). New Request review button on the spam alert in NotificationsPage: story owner clicks it, `requestSpamReview` stamps `stories.spamReviewRequestedAt` (new schema field) and logs `spam.reviewRequested` to the Activity tab with the submitter as actor, so disputes surface even when the reason email bounces. Idempotent (one request per mark); the button flips to a persistent "Review requested {time}" chip driven by the owner-scoped `getMySpamStatus` query (null for non-owners/signed-out so the alert quietly skips the button). Admin side: amber Flag "Review requested" badges in scan results and the Marked spam review, disputed rows sort to the top of the review list, and a new Dismiss action (`dismissSpamReviewRequest`, moderation.moderate, logs `spam.reviewDismissed`) clears the request while keeping the mark; Unmark clears it too. Spam email and notification copy now mention the in-app button; Admin Docs gained a "Disputes and review requests" section. Verified: convex codegen green, app tsc zero errors in touched files, zero lints. PRD: prds/spam-request-review.md. Files: convex/schema.ts, convex/spamCheck.ts, convex/emails/spam.ts, src/pages/NotificationsPage.tsx, src/components/admin/SpamCheck.tsx, src/components/admin/AdminDocs.tsx.

- [x] 2026-08-13 19:10 UTC: Spam automation agent, marked-date filter, and AI audit trail on the AI Spam tab. New Automation card (moderation.moderate to change, all enforced server side via appSettings): Auto-scan new submissions toggle (default on, gated inside autoScanStory so submit mutations are untouched), Agent auto-mark spam toggle (default off, confirm-dialog gated; when an automatic scan returns a spam verdict at or above the confidence threshold the agent marks + hides in the same saveResult transaction; manual/batch scans never auto-mark so re-scanning old content cannot mass-hide), Confidence threshold input (default 85, clamped 50-100 server side), and Notify submitter toggle (default on; off marks silently for review). New stories.spamMarkedByAgent flag set on auto-mark and cleared on unmark; "Auto-marked spam" Bot badges in scan results and the Marked spam review, "by AI Spam Agent" in the marked row meta. Auto-marks and settings changes log to the Activity tab (spam.autoMarked with actor "AI Spam Agent" + confidence/reasons, spam.automationUpdated). Marked spam review gained a marked-date DateRangePicker (server-side filter on spamMarkedAt, persisted to localStorage like the other two ranges) with a range-aware empty state. Auto-mark emails still respect the global email kill switch and spam_notification type toggle. Admin Docs AI spam check section rewritten (Automation + Reviewing marked spam sections). Verified: convex codegen green (uploads + TS bindings), app tsc zero errors in touched files, zero lints. PRD: prds/spam-automation-agent.md. Files: convex/schema.ts, convex/spamCheck.ts, src/components/admin/SpamCheck.tsx, src/components/admin/AdminDocs.tsx.

- [x] 2026-08-13 18:05 UTC: Marked spam review with bulk delete on the AI Spam tab. New "Marked spam" section (collapsed behind a Review marked spam toggle) lists every story with isSpam true straight from the stories table via a new `by_isSpam` index and `listMarkedSpam` query (moderation.view), so marked stories without a scan row can no longer hide from review. Rows show title, live URL link, author, submitted/marked times, marking admin, and the reason sent; each row has Unmark (existing mutation) and the section has its own select all + Delete selected (moderation.delete) running the existing `bulkDelete` cascade in sequential chunks of 50, so selections larger than the 50-id mutation cap no longer throw; the scan-results bulk delete bar uses the same chunked helper now. Query only runs while the section is open (skip pattern). Verified: convex codegen green, tsc clean on all touched files (remaining errors pre-existing elsewhere), zero lints. PRD: prds/marked-spam-review-bulk-delete.md. Files: convex/schema.ts, convex/spamCheck.ts, src/components/admin/SpamCheck.tsx.

- [x] 2026-08-13 09:00 UTC: Fixed story pages 404ing on refresh after the per-app markdown launch. Root cause: Netlify placeholders/splats only match whole path segments, so the `/s/:slug.md` and `/s/*.md` proxy rules over-matched and proxied plain `/s/{slug}` story URLs to the Convex `/s/*` HTTP action (404). Per-app markdown moved to `/md/{slug}.md` with a safe `/md/*` proxy rule; `/s/{slug}/llms.txt` unchanged; Convex serves both prefixes and keeps legacy `/s/{slug}.md` on its own host; botMeta edge function now only intercepts single-segment `/s/{slug}` bot requests so crawlers can fetch `/s/{slug}/llms.txt`. Sidebar, site `/llms.txt`, `/vibeapps.md`, sitemap, and robots point at the new path. Verified on dev .site: `/md/{slug}.md` 200 text/markdown, `/s/{slug}/llms.txt` 200 text/plain, missing slug 404, `/llms.txt` lists `/md/` links; convex codegen TypeScript green, zero lints. Needs Netlify deploy (git push) plus `npx convex deploy` to land on prod. PRD: prds/per-submission-llms-and-md.md. Files: public/_redirects, netlify/edge-functions/botMeta.ts, convex/siteDirectory.ts, convex/http.ts, src/components/StoryDetail.tsx, public/robots.txt.

- [x] 2026-08-13 08:41 UTC: Per-submission `llms.txt` and markdown files from the story slug. Public apps (approved, not hidden, not spam, not archived) get live `/s/{slug}/llms.txt` and `/s/{slug}.md`. Story sidebar shows both links above View Change Log. Site `/llms.txt` and `/vibeapps.md` (footer) list those URLs per app; sitemap includes them too. Unpublished slugs 404. Verified: convex codegen TypeScript green, curl 200 text/plain and text/markdown on a public slug, 404 on a missing slug, site directory files list the per-app links. PRD: prds/per-submission-llms-and-md.md. Files: convex/siteDirectory.ts, convex/siteFiles.ts, convex/http.ts, public/_redirects, src/components/StoryDetail.tsx.

- [x] 2026-08-13 08:10 UTC: Editable judging group URL slug after create. Pencil next to `/judging/{slug}` in the workspace header (and Change slug in Settings) opens a site-design warning dialog; `updateGroupSlug` sanitizes, enforces uniqueness via `by_slug`, patches, logs `judgingGroup.slugChanged`, and the client replace-navigates to the new admin URL keeping `?section=`. Public judging/submit/results/AI results pages, Links ledger, and Agent API all look up the current slug so they follow immediately; old URLs 404. New delegated permission `judging.slug` (destructive) in Access Management; full Clerk admins bypass. Admin Docs cover the warning and grant. Verified: convex codegen TypeScript green, eslint 0 errors on touched files. PRD: prds/judging-group-editable-slug.md. Files: convex/adminAccess.ts, convex/judgingGroups.ts, src/components/admin/AccessManagement.tsx, src/components/admin/judging/GroupSlugEditor.tsx (new), src/pages/AdminJudgingGroupPage.tsx, src/components/admin/judging/GroupSettingsSection.tsx, src/components/admin/AdminDocs.tsx.

- [x] 2026-08-13 07:57 UTC: Live auto-updating `/llms.txt` and `/vibeapps.md` directory files, plus AEO/SEO/GEO extras that keep existing Open Graph tags. Public submissions only (approved, not hidden, not spam, not archived), served live from Convex HTTP with a daily siteFiles fallback. Footer links added. Homepage JSON-LD, canonical, alternate markdown, AI-bot robots, and a dynamic sitemap of every public app. Verified: convex tsc exit 0, convex dev push green, curl of `/llms.txt` (32 apps, text/plain), `/vibeapps.md` (text/markdown), `/robots.txt`, `/sitemap.xml` all 200. PRD: prds/public-directory-llms-and-aeo.md. Files: convex/siteDirectory.ts (new), convex/siteFiles.ts, convex/http.ts, convex/crons.ts, convex/stories.ts, src/components/Footer.tsx, src/components/StoryDetail.tsx, index.html, public/_redirects, public/robots.txt, public/sitemap.xml.

- [x] 2026-08-13 07:40 UTC: Wider judging submit page, 16:9 header image, and required tag visibility. Single column layout on `/judging/:slug/submit` widened from max-w-2xl to max-w-4xl (matches the main submit page with the sidebar hidden) with the hero description at max-w-2xl. New `submissionPageImageAspect` on judgingGroups ("square" default | "wide"): a Square (1:1) / Wide (16:9) picker in the Header image block; wide images fill the layout width at 16:9 in both the single column hero and the two column sidebar, and the pixel size slider only renders for square. New `submissionFormRequiredTagVisible` on judgingGroups (unset = shown): a Shown/Hidden pill under the Required tag picker; hidden removes the locked tag from everything submitters see (Selected Tags pills, quick select buttons, tag counter, empty-state check) while the tag is still auto-selected and applied on submit so entries land in the group. Display only, so it never conflicts with Tag Management's isHidden flag (which keeps controlling story cards and tag limits; the admin hint calls out when the tag is also hidden there). Both fields flow through updateGroup args (generic copy loop), getGroupWithDetails, and getSubmissionPage. Admin Docs custom submission page section documents layouts, image shape, and required tag visibility. Verified: convex dev push green, zero lints on all touched files, app tsc errors all pre-existing (none in touched files). PRD: prds/judging-submit-page-width-and-required-tag-visibility.md. Files: convex/schema.ts, convex/judgingGroups.ts, src/pages/JudgingGroupSubmitPage.tsx, src/components/admin/judging/GroupSubmitPageSection.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-13 07:25 UTC: Judging group activity log with realtime review counts on removal. `removeSubmission` now deletes the group's `aiJudgeResults` rows along with the existing score/status/notes/completions cleanup, so overview stats, results rankings, and AI counts update in realtime via reactive queries; the removal entry records scoresDeleted and hadAiReview/aiReviewStatus (saveResult already no-ops on a deleted row, safe mid-run). Per-group audit trail on the shared `activityLog` table: new optional `groupId` field + `by_groupId` index, `logActivity` accepts groupId, and new events `judging.submissionAdded` (logged in `ensureStoryInGroup`, covering manual add, tag sync, auto-include, and the custom submit page; stories.submit auto-add path swapped to the shared helper), `judging.submissionRemoved`, `judging.aiRunStarted` (queued count), `judging.aiRetryQueued`, `judging.aiReviewCompleted`/`judging.aiReviewFailed` (actor "AI Judge", avg score in message); existing group create/update and score.submitted calls now carry groupId. New backend functions in convex/activityLog.ts: `listGroupActivity` (paginated, judging.view group scoped), `exportGroupActivity` (5000 cap), `clearGroupActivity` (judging.manage, 500/batch). New Activity sidebar section in the group workspace (below Judge tracking, visible to anyone who can open the page): event pill + message with submission link from metadata.storySlug, actor, relative time, Show 30/60/100 dropdown wired to loadMore, Load more button, Export CSV, Save as .md audit file, confirm-gated Clear noting rows also leave the site-wide log. Admin Docs updated (workspace table row + "The group Activity log" section). Verified: convex dev push green with `activityLog.by_groupId` index added, zero lints on all touched files, app tsc errors all pre-existing. PRD: prds/judging-group-activity-log.md. Files: convex/schema.ts, convex/activityLog.ts, convex/judgingGroupSubmissions.ts, convex/stories.ts, convex/aiJudge.ts, convex/judgingGroups.ts, convex/judgeScores.ts, src/components/admin/judging/GroupActivitySection.tsx (new), src/pages/AdminJudgingGroupPage.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-13 07:00 UTC: Admin form fields flow everywhere with per-group judging overrides. Fields added in Admin, Forms, Manage Form Fields now persist on every submission path instead of being dropped when they lack a dedicated stories column: new `stories.dynamicFormValues` array plus a shared resolver in convex/storyFormFields.ts (`resolveDynamicFieldValues`/`resolveDynamicFieldRecord`) that routes known keys to their column (hackathonLog sanitized) and everything else to the array; wired into stories.submit (new optional dynamicFieldValues arg), submitDynamic, and submitForms.submitFormData. Public dynamic submit forms auto-include enabled fields added after form creation (getPublicSubmitForm/getSubmitFormWithFields). Judging groups: new `submissionDynamicFieldOverrides` record with an "Additional form fields" list in the group Submit page section (per-field Required/Optional + Shown/Hidden, unset falls back to field defaults); form sections (team info, additional images, additional links) gained Required/Optional pills backed by new section keys on submissionFieldRequirements with submit-time validation; custom questions gained a Shown/Hidden pill via a new `visible` flag (hidden questions removed from the form and excluded from customFormAnswers, still group-scoped). Judges see dynamic values in a new "Additional Form Fields" card (getGroupSubmissions returns dynamicFormValues). Legacy Custom Forms sub-tab commented out in AdminDashboard with the subtab URL param coerced back to form-fields. Admin Docs custom submission page section rewritten for the new controls. Verified: convex dev push green, zero lints on all touched files. PRD: prds/dynamic-form-fields-judging-overrides.md. Files: convex/schema.ts, convex/validators.ts, convex/storyFormFields.ts, convex/stories.ts, convex/submitForms.ts, convex/judgingGroups.ts, convex/judgingGroupSubmissions.ts, src/components/StoryForm.tsx, src/pages/JudgingGroupSubmitPage.tsx, src/pages/JudgingInterfacePage.tsx, src/components/admin/judging/groupSection.tsx, src/components/admin/judging/GroupSubmitPageSection.tsx, src/components/admin/AdminDashboard.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-13 06:35 UTC: Public-repo hackathon.md judging alignment (follow-up to the 21:25 entry). Parser now extracts `whatItDoes` ("What it does:") and accepts the skill's "Convex features:" label as `features` in convex/hackathonLog.ts. The event badge works for repo-only submissions: `analyzeSubmission` parses the header once from the effective log (repo copy wins) and stores `hackathonLogEvent` on aiJudgeResults via saveResult (new optional schema field); enrichResults prefers the stored value and falls back to parsing a pasted log for older rows. No submit form changes needed for the public-only event; the built-in hackathonLog paste field stays disabled. Verified: parser test against the full spec sample header (all 13 fields incl. whatItDoes and Convex features; malformed input returns {} without throwing), convex tsc exit 0, convex dev push green, zero lints. PRD: prds/hackathon-md-single-file-skill.md. Files: convex/hackathonLog.ts, convex/schema.ts, convex/aiJudge.ts, convex/aiJudgeAnalysis.ts
- [x] 2026-08-12 21:25 UTC: Single-file hackathon skill support and hackathon skill API removal. Removed the `/api/hackathon/{slug}` HTTP API (openapi.json, rules.json, status, register, check, code auth, rate limiters), the backing functions in convex/hackathon.ts (only `normalizeProjectUrl`/`groupHasDuplicateUrl` remain), and the GroupHackathonSection admin UI; `hackathonRegistrations` and the judgingGroups hackathon fields stay in schema as deprecated optionals so prod rows remain valid. New paste path for private/no-repo teams: `stories.hackathonLog` (optional) reached through all four submission paths (submit, submitAnonymous, submitDynamic, submitFormData) via new convex/hackathonLog.ts (`sanitizeHackathonLog`: 20k cap with readable error, secret redaction for sk-/pk*/ghp*/gho*/github_pat*/xox/AKIA/JWT/Convex deploy keys to `[redacted]`); built-in disabled-by-default `hackathonLog` textarea field in storyFormFields (seeded on dev via ensureHackathonLogField), rendered in StoryForm/DynamicSubmitForm/JudgingGroupSubmitPage with monospace font, char counter, and judge-visibility warning; "textarea" added to fieldType unions and the admin field-type picker. AI judge: pasted log injected into PROJECT LOG FILES labeled "(pasted at submission; self-reported)" only when the repo returned no hackathon.md (repo copy wins with a one-line note; no-log submissions byte-identical), `parseHackathonLogHeader` deterministic header parser, three recorded-never-scored cross-checks in `aiJudgeResults.logDiscrepancies` (frontend claim vs detectFrontendHosting, components claim vs convex.config.ts scan, auth claim vs package.json deps map Clerk/WorkOS/Convex Auth/Better Auth), self-reported context sentences appended to buildSystemPrompt. Admin AI results show an event badge from the header and an expandable amber discrepancies indicator (admin-only via enrichResults includeLogMeta). Verified: convex dev push green, app tsc clean on touched files, zero lints, redaction + parser sanity test passed, hackathonLog field live on dev. PRD: prds/hackathon-md-single-file-skill.md. Files: convex/http.ts, convex/hackathon.ts, convex/hackathonLog.ts (new), convex/schema.ts, convex/stories.ts, convex/submitForms.ts, convex/storyFormFields.ts, convex/aiJudge.ts, convex/aiJudgeAnalysis.ts, convex/judgingGroups.ts, src/components/StoryForm.tsx, src/components/DynamicSubmitForm.tsx, src/pages/JudgingGroupSubmitPage.tsx, src/components/admin/FormFieldManagement.tsx, src/components/admin/AIJudgeResults.tsx, src/pages/AdminJudgingGroupPage.tsx, src/components/admin/judging/GroupHackathonSection.tsx (deleted)
- [x] 2026-08-12 19:05 UTC: AI judge frontend checker with per-platform hosting weights. New Frontend checker preset criterion in the Rubric weights card (explicit Add button like the components check; AI scores the deployed frontend 1-10) with five hosting platform sub-weights below it: Codex Sites, Convex static hosting, Vercel, Netlify, Other, each defaulting to 1 and adjustable 0-10. Hosting is detected deterministically during analysis (URL host suffixes .chatgpt.site / .convex.site / .convex.app / .vercel.app / .netlify.app, response headers x-vercel-id / x-nf-request-id / server, repo signals .openai/hosting.json / @convex-dev/self-static-hosting / vercel.json / netlify.toml, "other" fallback) and stored on the result as frontendHosting {platform, evidence}. The detected platform's weight multiplies the frontend-checker criterion weight in computeWeightedScore at read time, so weight edits re-rank instantly without re-running reviews. Prompt gets a FRONTEND HOSTING CHECK facts section; a dead live URL caps the frontend-checker score at 3; removing the criterion prunes its platform weights; admin results list shows a hosting platform badge. New schema fields judgingGroups.aiFrontendWeights and aiJudgeResults.frontendHosting; updateAiRubricWeights accepts frontendWeights (all-default clears storage); getGroupWithDetails returns aiFrontendWeights. Verified: tsc clean on all touched files (app + convex configs, remaining TS6133 warnings pre-existing), zero lints, convex dev push green. PRD: prds/ai-judge-frontend-checker-hosting-weights.md. Files: convex/schema.ts, convex/aiJudge.ts, convex/aiJudgeAnalysis.ts, convex/judgingGroups.ts, src/components/admin/judging/GroupAiSection.tsx, src/components/admin/judging/groupSection.tsx, src/components/admin/AIJudgeResults.tsx
- [x] 2026-08-12 18:45 UTC: Fixed GitHub issues 15 and 11. Issue 15: getUserProfileByUsername no longer leaks moderation data or shows rejected content on public profiles; the handler now filters fetched stories to status approved and not hidden (rejected stories kept isApproved true because updateStatus never cleared it) and strips rejectionReason, email, customMessage, spamReason, spamMarkedAt, spamMarkedBy, changeLog, and teamMembers before returning; updateStatus now keeps isApproved in sync with status. Issue 11: removing a last name no longer reverts on refresh; new users.nameCustomized flag set by updateProfileDetails on name change, ensureUser and syncUserFromClerkWebhook skip Clerk name sync when set, and the profile page sends an empty lastName to Clerk so it clears when the instance allows. Verified: eslint 0 errors, tsc no new errors, npm run build green, convex dev push green, live dev query confirmed no sensitive fields and only approved visible stories. PRD: prds/fix-github-issues-15-and-11.md. Files: convex/users.ts, convex/stories.ts, convex/schema.ts, src/pages/UserProfilePage.tsx
- [x] 2026-08-12 04:32 UTC: Judging group Emails can target submission owners as well as judges. Added a Send to SimpleSelect (Judges / Submission owners) on GroupEmailsSection; backend listGroupSubmissionOwnerRecipients resolves unique owners from group submissions (account email preferred, hidden/rejected skipped), and sendGroupEmail accepts recipientType plus storyIds while keeping the judges path. Same templates, preview, test send, schedule, and daily cap. Verified: convex codegen + TypeScript green on the module, app tsc clean on touched files, zero lints. PRD: prds/judging-group-email-submission-owners.md. Files: convex/emails/judgingGroupEmails.ts, src/components/admin/judging/GroupEmailsSection.tsx
- [x] 2026-08-11 20:05 UTC: Light theme canvas softened from pure white to soft grey (#f7f7f7) per feedback ("too much white"); white cards now read as raised surfaces against it. Files: src/index.css, .interface-design/system.md, changelog.md. Verified in browser: grey canvas with bordered white cards on the homepage.
- [x] 2026-08-11 19:20 UTC: Replaced the default Light theme with a pure white mono palette. src/index.css light tokens redone: canvas and surface #ffffff, surface-alt #f3f3f3, surface-hover #e8e8e8, ink/cta/brand #000000, copy #1f1f1f, soft #545454, faint #6b6b6b (4.9:1 on white), hairline #e2e2e2, hairline-strong #cccccc, brand-soft #ededed. No accent hue and no shadows in light mode; hairline borders carry all separation. Selected controls flip polarity so they survive the white canvas: view mode buttons (list/grid/vibe) use bg-cta border-cta with text-on-cta icons, the header "All" pill uses bg-cta text-on-cta, sidebar selected category uses bg-surface-alt with ring; hover states moved from bg-surface to bg-surface-hover; borderless bg-surface cards gained border-hairline (RecentVibers, UserProfilePage, DynamicSubmitForm). Classic and Dark unchanged. Verified: tsc clean on touched files, zero lints, browser check of light homepage (white canvas, bordered cards, themed sort dropdown) and dark homepage (intact). Files: src/index.css, src/components/Layout.tsx, src/components/TopCategoriesOfWeek.tsx, src/components/RecentVibers.tsx, src/pages/UserProfilePage.tsx, src/components/DynamicSubmitForm.tsx, .interface-design/system.md, changelog.md
- [x] 2026-08-11 08:47 UTC: Themed dropdowns and confirm dialog keyboard support. All 18 single-value native `<select>` elements across 12 files replaced with a new `SimpleSelect` component (src/components/ui/SimpleSelect.tsx) wrapping the rethemed Radix `ui/select.tsx` (site tokens: surface, hairline, ink, copy, surface-hover), so open dropdown menus match the active theme in Light, Classic, and Dark instead of the OS default popup; full keyboard support (arrows, type-ahead, Enter, Escape) via Radix. Empty-string option values map through an internal sentinel; when "" means "nothing selected" (PublicForm placeholders) the placeholder renders instead. The `multiple` select in PublicForm stays native by design (inline listbox, not an OS popup). AlertDialog ("Mark as spam?" and other destructive confirms) now autofocuses Cancel on open, traps Tab/Shift+Tab between Cancel and Confirm, activates the focused button on Enter, and sets aria-modal; MessageDialog and PromptDialog got aria-modal too. Verified: tsc clean on touched files (remaining warnings pre-existing), zero lints, browser check confirmed themed panels + keyboard flow in Light and Dark. PRD: prds/themed-dropdowns-and-dialog-keyboard.md. Files: src/components/ui/select.tsx, SimpleSelect.tsx (new), AlertDialog.tsx, MessageDialog.tsx, PromptDialog.tsx, src/components/Layout.tsx, src/pages/NavTestPage.tsx, src/pages/JudgingInterfacePage.tsx, src/components/PublicForm.tsx, src/components/admin/Settings.tsx, AIJudgeResults.tsx, TagManagement.tsx, FormBuilder.tsx, FormFieldManagement.tsx, EditJudgingGroupModal.tsx, src/components/admin/judging/GroupEmailsSection.tsx, GroupSubmitPageSection.tsx
- [x] 2026-08-11 08:35 UTC: Admin setting to hide the /submit right sidebar and widen the form. New `hideSubmitPageSidebar` boolean on the settings singleton (schema + DEFAULT_SETTINGS + get fallback + update arg in convex/settings.ts, default false, settings.manage to change). Layout.tsx folds the flag into the showSidebar gate for the exact /submit path only (dynamic /submit/:slug pages already hide it), so the main column goes w-full; StoryForm.tsx widens its container from max-w-2xl to max-w-4xl when the flag is on (it already subscribed to api.settings.get). New "Submit Page Layout" checkbox section in admin?tab=settings saved through the existing Save Changes flow. Verified: convex dev push green, zero lints. PRD: prds/submit-page-sidebar-setting.md. Files: convex/schema.ts, convex/settings.ts, src/components/Layout.tsx, src/components/StoryForm.tsx, src/components/admin/Settings.tsx
- [x] 2026-08-11 08:30 UTC: Fixed user profile ReturnsValidationError for spam-marked submissions. getUserProfileByUsername (and listUserStories) crashed with "Value does not match validator" when a profile contained a story an admin marked as spam: stories.\* spread the raw doc which now carries isSpam/spamReason/spamMarkedAt/spamMarkedBy, but baseStoryValidator in convex/validators.ts was never updated when the AI spam fields were added to the schema. Added the four optional fields to baseStoryValidator and StoryWithDetailsPublic. Verified: convex dev push green ("Convex functions ready"), zero lints. Files: convex/validators.ts
- [x] 2026-08-11 08:05 UTC: Three-theme system and view refresh. Light (new default), Classic (previous look preserved), and Dark themes driven by CSS variables on `html[data-theme]` (src/index.css) exposed as semantic Tailwind colors (canvas, surface, ink, copy, soft, faint, hairline, cta, on-cta, brand) in tailwind.config.js; pre-paint script in index.html prevents theme flash. New ThemeProvider/useTheme (src/lib/ThemeContext.tsx, localStorage key vibeapps-theme) and Phosphor icon ThemeToggle next to the profile icon in the header, desktop + mobile; Clerk modals get dark baseTheme and sonner Toaster is mounted in App.tsx themed per active theme. A one-off codemod (since deleted) converted ~1,700 hardcoded hex utility classes across 79 files (admin included) to token classes, followed by a manual contrast/border audit: tag chip fallback colors moved to CSS variables (DB-stored tag colors untouched), text-white to text-on-cta on themed CTAs, filter pills use ring-offset-canvas, moderation buttons and yellow highlight tints made theme-aware, dark hairline opacity raised to 0.14 for card visibility. StoryList list view redesigned as ranked Product Hunt / new Digg style rows (rank number, app thumbnail, title, tagline, tag pills, meta row, upvote box right); vibe view modernized in the same style; grid view unchanged. Verified: tsc clean on touched files (remaining errors pre-existing), eslint 0 errors, browser check of all three themes across list/grid/vibe, story detail, admin tabs, sign-in modal, and 375px mobile. PRD: prds/theme-system-and-view-refresh.md. Files: src/index.css, tailwind.config.js, index.html, src/lib/ThemeContext.tsx (new), src/components/ThemeToggle.tsx (new), src/main.tsx, src/App.tsx, src/components/Layout.tsx, src/components/StoryList.tsx, plus 70+ files token-converted under src/
- [x] 2026-08-11 04:55 UTC: Web Interface Guidelines UI audit. Reviewed all 103 files under src/ against the Vercel Web Interface Guidelines (fetched fresh from vercel-labs/web-interface-guidelines): pattern scans across the whole tree plus full reads of index.html, the ui/ primitives, Layout.tsx, and StoryList.tsx. High-impact findings: shared Button (ui/button.tsx:13) removes the focus outline with no focus-visible replacement and its asChild span drops keyboard semantics; the custom dialog (ui/dialog.tsx) lacks role="dialog"/aria-modal, focus trap, and overscroll-behavior: contain; zero autoComplete attributes exist app wide; no prefers-reduced-motion handling anywhere; "..." instead of "…" in ~40 files; no img sets width/height (CLS); transition-all in ~10 files; Layout header search inputs and selects lack labels; StoryList vote buttons lack aria-label. Passing: no browser confirm/alert, no paste blocking, zoom not disabled, correct input types on existing email/url fields, ui/input.tsx and checkbox.tsx focus-visible pattern correct. No code changed this session (review only, per the skill); full findings with file:line references and a 6-step fix order live in the PRD. PRD: prds/web-interface-guidelines-audit.md
- [x] 2026-08-11 01:50 UTC: Convex docs and insights follow-up check. Reviewed docs.convex.dev/llms.txt (docs index only, no new Resend or email guidance affecting this app; component setup already matches the Resend component docs) and confirmed @convex-dev/resend 0.2.6 is the latest published version. Investigated npx convex insights OCC warnings (spamWorkpool/workpool complete.js:complete and loop.js:updateRunStatus on internal tables pendingCompletion, runStatus, pendingStart): these come from inside the @convex-dev/workpool component when parallel jobs finish simultaneously, were auto-retried successfully (1-4 conflicts over 72h, down to 2 single-conflict warnings at recheck), touch no app tables, and need no app code change per the convex-write-conflicts skill thresholds. Optional later: upgrade @convex-dev/workpool 0.3.2 to 0.4.x for internal contention improvements (breaking-ish jump, not worth it mid email-enable). No code changes this session.
- [x] 2026-08-11 01:20 UTC: Resend email system audit and fixes before enabling email in admin. Audited every send path against the Resend docs and @convex-dev/resend component: all production sends route through emails/resend.sendEmail which enforces the global emailsEnabled kill switch and per-type emailTypeEnabled:{type} toggles (defaults off for submission_confirmation, submission_admin_alert, results_live, judging_group), webhook status sync via /resend-webhook + handleEmailEvent is wired correctly, and admin UI toggles read/write the same appSettings keys the backend enforces. Fixed four real bugs: (1) frozen cron date, crons.ts passed date computed at deploy time to processUserEngagement so engagement summaries were pinned to the deploy day and the 6 PM send found nothing; date arg is now optional and computed at run time in the action. (2) Unsubscribe links never rendered, six templates in emails/templates.ts had `${args.unsubscribeToken ? \` \` : ""}` (a literal space); now render a real /api/unsubscribe?token= link. (3) vibeapps.dev/api/unsubscribe fell through to the SPA catch-all; added the Netlify _redirects proxy rule to the Convex site domain. (4) List-Unsubscribe-Post advertised one-click but only GET existed; added POST /api/unsubscribe (RFC 8058). Also cleaned the always-false marketingEmails ternary in unsubscribe.ts. Prod blockers documented for the user: RESEND_API_KEY and RESEND_WEBHOOK_SECRET are set on dev (acoustic-goldfinch-461) but missing on prod, and the prod webhook endpoint must be registered in the Resend dashboard. Verified: convex dev push green (validates cron args + function types), app tsc clean on touched files (remaining errors pre-existing), zero lints. PRD: prds/resend-email-audit.md. Files: convex/crons.ts, convex/emails/daily.ts, convex/emails/templates.ts, convex/emails/unsubscribe.ts, convex/http.ts, public/_redirects
- [x] 2026-08-10 22:20 UTC: Group link variables in email templates. Three new template variables resolving to the group's real share links (same URLs as the Links section): {{judgingurl}} -> /judging/{slug}, {{resultsurl}} -> /judging/{slug}/results, {{submissionurl}} -> /judging/{slug}/submit, built by a new shared judgingGroupUrls(slug) helper in convex/emails/render.ts used by the backend delivery action and both admin previews. renderInline now autolinks bare https URLs (skips URLs already inside anchors, keeps trailing punctuation outside the link) so a bare {{judgingurl}} is clickable, while [text]({{judgingurl}}) markdown links still work. deliverGroupEmails takes a required groupSlug arg passed from sendGroupEmail/sendGroupTestEmail (feature is unreleased so no queued scheduled sends carry old args); per-recipient vars spread the group URLs. GroupEmailsSection preview uses judgingGroupUrls(group.slug); EmailTemplatesManager PREVIEW_VARS uses sample fall-hackathon links; both variables legends pick up the new keys automatically from TEMPLATE_VARIABLES. Verified: runtime render test via tsx (bare autolink with trailing period outside anchor, markdown link not double-linked, unknown vars preserved, HTML escaped), convex push green, app tsc clean on touched files, eslint clean, prettier unchanged. PRD: prds/judging-group-emails.md. Files: convex/emails/render.ts, convex/emails/judgingGroupEmails.ts, src/components/admin/judging/GroupEmailsSection.tsx, src/components/admin/EmailTemplatesManager.tsx
- [x] 2026-08-10 20:45 UTC: AI Spam counts strip filters results. The count pills under "Scan results" (scanned, spam, suspicious, clean, marked, failed) are now clickable quick filters via a new CountPill button component: clicking a pill applies that verdict filter to the results list (same verdictFilter state the dropdown uses, so both stay in sync), clicking the active pill again clears back to all, and "scanned" always resets to all. Active pill gets a ring + shadow and aria-pressed for accessibility; the "in progress" pill stays informational since pending/running has no list filter. Verified: app tsc clean on touched files, zero lints. Files: src/components/admin/SpamCheck.tsx
- [x] 2026-08-10 20:25 UTC: Judging group emails round two: scheduling, daily cap, delivery stats, real-judge preview. Scheduled sends: sendGroupEmail takes optional scheduledAtMs (min now+1m, max 30 days), inserts a groupScheduledEmails row (new schema table with by_groupId index, statuses pending/sent/cancelled), queues delivery with ctx.scheduler.runAt, and stores the scheduled function id so cancelScheduledEmail can cancel it (idempotent); delivery marks the row sent via internal markScheduledEmailSent, and listScheduledEmails feeds a Scheduled sends card with confirm-gated cancel. Daily cap: GROUP_DAILY_RECIPIENT_CAP = 200 recipients per group per rolling 24h counted from non-test judging_group emailLogs (by_type_date range) plus pending scheduled recipients, enforced in sendGroupEmail with a remaining-count error; getGroupEmailStatus takes a client-supplied now arg (queries stay deterministic) and returns dailyCap/usedLast24h, shown as a usage line with over-cap send disable. Per-send stats: deliverGroupEmails stamps a shared sendId in metadata, listGroupSends (replaces listGroupEmailLog) groups the last 500 group logs by sendId into delivered/opened/bounced/failed/complained counts rendered as badges per send; Resend webhook handleEmailEvent now records the first email.opened as metadata.openedAt without touching the status union. Preview as real judge: compose preview substitutes a selected judge's actual name/email with a picker when more than one recipient is selected. Verified: convex push green (groupScheduledEmails.by_groupId added), app tsc clean on touched files, eslint clean (one pre-existing warning untouched), prettier applied. PRD: prds/judging-group-emails.md. Files: convex/schema.ts, convex/emails/judgingGroupEmails.ts, convex/emails/queries.ts, src/components/admin/judging/GroupEmailsSection.tsx
- [x] 2026-08-10 19:50 UTC: AI Spam tab date ranges persist. Both DateRangePickers on the AI Spam tab (the "Run a scan" range and the view-only "Scan results" filter range) are now saved to localStorage (keys adminSpamScanRange, adminSpamFilterRange, stored as {from, to} ms timestamps) and restored via lazy useState initializers, so picked ranges survive admin tab switches and page reloads; clearing a range removes the saved value. Verified: app tsc clean on touched files, zero lints. Files: src/components/admin/SpamCheck.tsx
- [x] 2026-08-10 20:10 UTC: Judging group emails with reusable templates. New Emails section in the judging group workspace (permission `judging.emails`, added to PERMISSION_KEYS and the Access tab's Judging card, scoped by group like other judging keys): template picker, editable subject + markdown-lite body + optional signature, reply-to input with group notificationEmails as datalist suggestions, judge recipient checklist (judges with an email, deduplicated by lowercased address, select all default), per-recipient variable preview in a sandboxed iframe, "Send test to me", confirm-gated send, blocked-state banner, and a recent sends list read from emailLogs by the by_type_date index filtered on metadata.groupId. New Templates sub tab in Email Management (existing page moved unchanged under a Send & Settings sub tab): emailTemplates table CRUD (name, subject, body, optional signature; edit needs emails.send, list also allows judging.emails) with variables legend and live preview. New shared dependency-free renderer convex/emails/render.ts ({{firstname}}/{{name}}/{{email}}/{{groupname}} substitution, HTML-escape-first markdown-lite: bold, italic, links, lists, paragraphs; branded shell with signature divider) imported by both the backend delivery action and the frontend previews so rendering never drifts. New email type judging_group (schema emailLogs union + shared registry, default off) with a toggle row under a new Judging group in Email Send Options; sends route through emails/resend.sendEmail so the master switch and per-type toggle are enforced server side, with friendly pre-send errors in the mutation. Delivery runs as a scheduled internal action in batches of 10 with 1s pauses, logging subject/groupId/sender/templateId/isTest in metadata. Verified: convex codegen + push green, app tsc clean on touched files, zero lints, prettier applied. PRD: prds/judging-group-emails.md. Files: convex/emails/render.ts (new), convex/emails/judgingGroupEmails.ts (new), convex/emailTemplates.ts (new), convex/emails/emailTypes.ts, convex/schema.ts, convex/adminAccess.ts, src/components/admin/EmailTemplatesManager.tsx (new), src/components/admin/judging/GroupEmailsSection.tsx (new), src/components/admin/EmailManagement.tsx, src/pages/AdminJudgingGroupPage.tsx, src/components/admin/AccessManagement.tsx
- [x] 2026-08-10 19:25 UTC: AI Spam tab two-step layout. Split the confusing shared date range (it filtered the list AND silently scoped scans) into two independent ranges: a "Run a scan" card at the top with its own DateRangePicker (placeholder "All recent submissions") plus Scan / Re-scan buttons whose labels switch to "Scan this range" / "Re-scan this range" when a range is picked, and a "Scan results" section whose verdict/sort/date filters are view-only (never trigger scans). Added inline microcopy on both sections explaining auto-scan on new submissions, the 100-per-run cap, and scan vs re-scan; empty state now differs when filters are active (suggests clearing them or scanning that period). AI prompt button stays in the header. Verified: app tsc clean on touched files, zero lints. Files: src/components/admin/SpamCheck.tsx
- [x] 2026-08-10 19:15 UTC: AI Spam Check date range picker. Replaced the two native `<input type="date">` fields on the AI Spam tab (broken-looking OS calendar buttons) with a site-styled DateRangePicker: single trigger button with formatted range label and inline clear, popover containing preset windows (Last 7 days, Last 30 days, This month, Last month, Last 3 months) and a two-month react-day-picker range calendar styled with the site palette (#292929 endpoints, #F4F2EE range fill). SpamCheck state simplified from two strings to one DateRange; start is normalized to start of day and end to end of day (single-day picks cover that full day), still scoping both listSpamResults and the batch scan buttons. New reusable primitives: src/components/ui/popover.tsx, calendar.tsx, date-range-picker.tsx; new deps react-day-picker + @radix-ui/react-popover. Verified: app tsc clean on touched files (remaining errors pre-existing), zero lints, Vite HMR compiled with new deps optimized. Files: src/components/ui/popover.tsx (new), src/components/ui/calendar.tsx (new), src/components/ui/date-range-picker.tsx (new), src/components/admin/SpamCheck.tsx, package.json
- [x] 2026-08-10 07:00 UTC: Submission emails and per-email-type send toggles. Every email type now has an admin toggle: new shared registry convex/emails/emailTypes.ts (EMAIL_TYPES union, validator, EMAIL_TYPE_DEFAULTS, `emailTypeEnabled:{type}` appSettings key helper) enforced centrally in emails/resend.sendEmail right after the global emailsEnabled check (no stored row falls back to the default; disabled types skip without logging a send), so all 10 existing senders and crons needed zero per-trigger changes. New settings functions: getEmailTypeSettings (settings.view or emails.view, returns effective booleans for the dashboard in one query) and setEmailTypeEnabled (emails.send, logs settings.emailTypeToggled). Three new types, default off: submission_confirmation (receipt to the submitter scheduled from stories.submit when args.email present, story link, group-aware copy), submission_admin_alert (emails judgingGroups.notificationEmails when a submission lands in a group), results_live (admin-triggered blast of the public results URL to every de-duplicated submitter email in a group, account email preferred, recipients keyed lowercase). New convex/emails/submissions.ts holds contexts, the three internalActions, getResultsLiveEmailStatus (judging.manage; master/type toggle state + recipient count), and queueResultsLiveEmails mutation (requires resultsIsPublic, schedules the blast). Schema: 3 new emailLogs emailType literals, judgingGroups.notificationEmails; updateGroup accepts notificationEmails (null clears) and getGroupWithDetails returns it. UI: Email Send Options card in EmailManagement (grouped toggle rows, disabled with notice while the master switch is off, toggle-state hints on the Automated info card), Submission notifications card in GroupSettingsSection (email chip list with validation and de-dupe), Email all submitters block in GroupAccessSection's Results visibility card (recipient count, two-step confirm, disabled with Email-dashboard hint when master or results_live is off). Unsubscribe: List-Unsubscribe header + token for account holders, preferences footer on submitter emails; /resend-webhook already svix-verified via the component handler. Verified: convex dev push green, app tsc clean on touched files, zero lints. Plan: .cursor/plans/resend_submission_email_notifications_8adc067d.plan.md. Files: convex/emails/emailTypes.ts (new), convex/emails/submissions.ts (new), convex/emails/resend.ts, convex/emails/queries.ts, convex/settings.ts, convex/stories.ts, convex/judgingGroups.ts, convex/schema.ts, src/components/admin/EmailManagement.tsx, src/components/admin/judging/GroupSettingsSection.tsx, src/components/admin/judging/GroupAccessSection.tsx
- [x] 2026-08-10 06:30 UTC: Hackathon skill support for judging groups. Judging groups can power the /hackathon agent skill without an API submit path (submissions stay on the group submit form). Schema: `hackathonSkillEnabled`, `hackathonRegistrationCodes`, `hackathonRules`, `hackathonRulesUpdatedAt` on judgingGroups plus new `hackathonRegistrations` table (by_groupId). New convex/hackathon.ts: `validateCode` (slug + code to group context, gated on the enable flag), idempotent `registerTeam`, `getRules` (rules markdown + criteria + AI rubric + dates + scoreScale + submit/results paths + updatedAt staleness marker), `getStatusForUrl` (normalized URL lifecycle with admin-safe under_review wording for hidden/spam), admin `updateHackathonSettings` (codes stored uppercase de-duplicated, rules edits bump updatedAt) and `listRegistrations`, plus `normalizeProjectUrl`/`groupHasDuplicateUrl` helpers. HTTP: `/api/hackathon/{slug}/` openapi.json (public), rules.json (ETag/If-None-Match), status?url=, POST register, POST check (deterministic pre-submit: event window, live URL GET, hackathon.json fetch/parse with required-field check, duplicate URL, nothing persisted, no LLM); code auth via x-hackathon-code header, Bearer, or ?code=; rate limits 60 reads/min and 10 checks/min per code. Duplicate guard in stories.submit rejects a project URL already submitted to the same group (hidden/rejected entries free the URL); JudgingGroupSubmitPage shows Convex errors clean and supports ?url=&title=&tagline=&github= prefill. Admin: new Hackathon skill sidebar section (judging.manage) with enable toggle, codes list, rules markdown editor, endpoint URL ledger, and registered teams card; getGroupWithDetails/updateGroup carry the new fields. AI judge and human judging unchanged. Verified: convex dev push green, app tsc clean on touched files, zero lints. Plan: .cursor/plans/hackathon_skill_support_endpoints_14e93513.plan.md. Files: convex/hackathon.ts (new), convex/schema.ts, convex/http.ts, convex/stories.ts, convex/judgingGroups.ts, src/components/admin/judging/GroupHackathonSection.tsx (new), src/pages/AdminJudgingGroupPage.tsx, src/pages/JudgingGroupSubmitPage.tsx
- [x] 2026-08-10 06:20 UTC: Admin activity log. New `activityLog` table (by_archived, by_archived_category indexes) plus `convex/activityLog.ts` with a pause-aware `logActivity` helper (reads appSettings `activityLogPaused`, resolves actor from auth or explicit override, try/catch so logging never breaks the host mutation) wired into: emails/queries.insertEmailLog (every send attempt, System actor), stories submit/submitAnonymous (submissions), stories updateStatus/hideStory/showStory/deleteStory (moderation), spamCheck markAsSpam/unmarkSpam/bulkMarkAsSpam/bulkHide/bulkDelete/startBatchScan, judgingGroups createGroup/updateGroup/deleteGroup, judgeScores.submitScore (new scores only, judge name as actor), adminAccess grantAccess/revokeAccess, settings update/toggleEmails. API: paginated listActivity (category/archived/sort filters), getStatus/setPaused, bulkArchive/bulkUnarchive/bulkDelete, batched clearLog (500/call, frontend loops), exportActivity (5000 row cap). Two new permission keys activity.view/activity.manage in PERMISSION_KEYS, grantable via a new Activity section in AccessManagement. New Activity admin tab (ActivityLog.tsx): category pills, Active/Archived toggle, newest/oldest sort, pause/resume with banner, CSV export, confirm-gated clear, select all, bulk archive/restore/delete, expandable metadata rows, load more. Verified: convex codegen + convex tsc clean, convex dev push green, app tsc clean on touched files, zero lints. PRD: prds/admin-activity-log.md. Files: convex/schema.ts, convex/activityLog.ts (new), convex/adminAccess.ts, convex/emails/queries.ts, convex/stories.ts, convex/spamCheck.ts, convex/judgingGroups.ts, convex/judgeScores.ts, convex/settings.ts, src/components/admin/ActivityLog.tsx (new), src/components/admin/AdminDashboard.tsx, src/components/admin/AccessManagement.tsx
- [x] 2026-08-10 06:00 UTC: Resend component upgrade and webhook status fix. Upgraded @convex-dev/resend 0.2.5 to 0.2.6 (forwards message_id from Resend webhook payloads to onEmailEvent). Fixed the /resend-webhook route: the hand-rolled handler had no svix signature verification and matched Resend's message_id against emailLogs.resendMessageId, which stores the component email id from resend.sendEmail, so delivered/bounced/complained statuses never updated. Route now delegates to resend.handleResendEventWebhook (verifies with RESEND_WEBHOOK_SECRET, already set on dev), and a new onEmailEvent internal mutation emails/queries.handleEmailEvent syncs emailLogs by the component id via by_resend_id. Also typed the send payload in emails/resend.ts (dropped any, replyTo now string[] per SendEmailOptions). Webhook URL unchanged, no Resend dashboard change needed; admin Email Management (test email, broadcasts to all/selected/tag) verified to route through the logged component send path. Verified: convex tsc clean, convex dev push green, zero lints. PRD: prds/resend-component-upgrade-webhook-fix.md. Files: package.json, convex/sendEmails.ts, convex/emails/queries.ts, convex/emails/resend.ts, convex/http.ts
- [x] 2026-08-10 05:45 UTC: Admin Docs refresh for agent judges and AI spam check. Rewrote the Agent judges doc section into a full integration guide: three-step setup (toggle, key, Links URLs), x-judge-key and Bearer auth examples, endpoint table, POST /scores payload example with update-on-repost and complete flag notes, per-key rate limits (120 reads/min, 30 writes/min, verified against convex/http.ts agentApiLimiter), and a 401/403/429 error table. Added a new AI spam check doc section (scan triggers incl. date range, measured signals, editable prompt with reset, review flow, mark/unmark behavior with alert + email, kill switch note, moderation permissions) with ShieldAlert icon. Env vars section renamed to cover both features and now lists ADMIN_EMAIL. Verified: zero lints. Files: src/components/admin/AdminDocs.tsx.
- [x] 2026-08-10 05:30 UTC: AI Spam Check prompt editor, spam notification copy, email toggle audit. (1) The spam verdict system prompt is now admin-editable: default moved to `DEFAULT_SPAM_SYSTEM_PROMPT` in convex/spamCheck.ts with `getSpamPrompt` (moderation.view), `setSpamPrompt` (moderation.moderate, empty or default text clears the override), and `getSpamPromptInternal` for the analysis action, stored under appSettings key `spamCheckSystemPrompt`; SpamCheck.tsx adds an AI prompt button (custom badge when overridden) opening an editor with save, confirm-gated reset to default, and close. (2) Spam alerts no longer fall through to "Someone interacted with your content": Layout dropdown gets a spam case, NotificationsPage renders "Your post has been marked as spam and has been removed" with a link to https://github.com/waynesutton/vibeapps/issues, and the spam email adds the same issues link next to reply-to. (3) Verified spam emails were already gated by the global `emailsEnabled` kill switch in emails/resend.sendEmail; only explicit admin test-email actions bypass it. Verified: Convex dev push clean, zero lints. PRD: prds/ai-spam-detection-admin.md. Files: convex/spamCheck.ts, convex/spamCheckAnalysis.ts, convex/emails/spam.ts, src/components/admin/SpamCheck.tsx, src/components/Layout.tsx, src/pages/NotificationsPage.tsx.
- [x] 2026-08-10 05:15 UTC: AI Spam Check date range. Added a start/end date filter to the AI Spam admin tab that scopes both the results list (server-side filter on story submission time in `listSpamResults`) and the batch scan buttons (`startBatchScan` accepts optional `startDate`/`endDate` and queries the `by_creation_time` index for that window, still capped at 100). End date is inclusive through end of day; a Clear dates link resets the range. Verified: Convex dev push clean, zero lints. PRD: prds/ai-spam-detection-admin.md. Files: convex/spamCheck.ts, src/components/admin/SpamCheck.tsx.
- [x] 2026-08-10 01:10 UTC: AI spam detection with admin review tab. New submissions get an automatic AI spam scan (scheduled fire-and-forget from stories.submit and submitAnonymous), and a new AI Spam admin tab (moderation.view) supports batch scanning the 100 most recent submissions plus per-row re-scans. Scans run in a dedicated `spamWorkpool` (parallelism 3, isolated from the AI judge pool) and combine deterministic signals (live URL GET with timeout, Firecrawl component scrape via `@firecrawl/firecrawl-convex`, GitHub repo reachability and file count with empty-repo detection via optional GITHUB_TOKEN, duplicate URL count via new by_url index, video/LinkedIn/X link liveness as weak signals) with an AI verdict (spam/suspicious/clean, confidence, reasons) using the Anthropic → OpenAI → OpenRouter fallback chain; a deterministic heuristic scores hard signals when no AI key is set. Marking as spam (single or bulk, moderation.moderate) hides the story, stamps isSpam/spamReason/spamMarkedAt/spamMarkedBy, sends an in-app "spam" alert, and emails the submitter the reason (new spam_notification email type, reply-to ADMIN_EMAIL when set, account email preferred over form email, silent skip for anonymous without email); unmark reverses everything. Bulk bar also offers hide and permanent delete (moderation.delete; cascades comments, votes, ratings, bookmarks, scan rows, storage images). Tab has live counts, verdict filter, sort (newest/oldest/confidence), select all, optional custom reason input, and expandable rows with full AI reasoning and per-signal breakdown; Moderation tab shows a red Spam badge on confirmed rows and NotificationsPage renders the new alert type. New schema: spamCheckResults table (by_storyId/by_status/by_verdict), stories spam fields + by_url index, alerts "spam" type, emailLogs "spam_notification" type. Verified: convex dev push green, convex tsc clean, app tsc clean on touched files (remaining errors pre-existing), zero lints. PRD: prds/ai-spam-detection-admin.md. Files: convex/spamCheck.ts (new), convex/spamCheckAnalysis.ts (new), convex/emails/spam.ts (new), convex/schema.ts, convex/convex.config.ts, convex/stories.ts, convex/alerts.ts, convex/emails/resend.ts, convex/emails/queries.ts, src/components/admin/SpamCheck.tsx (new), src/components/admin/AdminDashboard.tsx, src/components/admin/ContentModeration.tsx, src/pages/NotificationsPage.tsx
- [x] 2026-08-10 00:45 UTC: Judging links event kit and submit password fix. Fixed a submit page password bypass: JudgingGroupSubmitPage auto-unlocked whenever the group's judge access flag isPublic was true, skipping the submission password gate on public-judge groups; removed the effect so the gate always shows when a submission password is set (password-free pages still open directly since the gate only renders when hasSubmissionPagePassword). Links section upgrades for handing a group to external organizers: copy-all and .md download now include the decoded password under each locked link (stored passwords are base64; getGroupWithDetails is admin gated; aiResultsPassword added to its return so all four links are covered; passwords never render in on-screen rows), a hint appears when the custom submission page is off pointing to the Submit page section, and a score scale note (human 1 to N vs AI always 1 to 10) shows when the AI judge is on, in both the card and the export. Verified: app tsc clean on touched files (remaining errors pre-existing), zero lints. PRD: prds/judging-links-event-kit-and-submit-password-fix.md. Files: src/pages/JudgingGroupSubmitPage.tsx, convex/judgingGroups.ts, src/components/admin/judging/GroupLinksSection.tsx
- [x] 2026-08-09 19:15 UTC: Per-group customizable submission forms. The group workspace Submit page section now controls the whole public form: show/hide toggles for every built-in field and section (title always shown), per-field required/optional, and a custom questions editor (text/url/email/textarea with label, placeholder, description, required; slug-style keys generated from labels). New schema fields `submissionFieldVisibility` + `submissionCustomQuestions` on judgingGroups and `customFormAnswers` (key/label/value, label denormalized) on stories. Public form renders conditionally from the stored config, sends custom answers on submit, and `stories.submit` applies the group's required tag server-side as a backstop when the tags picker is hidden. Judges see custom answers in the judging interface; `getGroupSubmissions` returns them. Guard rails: hiding tags requires a required tag, warning shown when GitHub URL is hidden with AI judge on, custom question keys validated unique with non-empty labels. Also fixed latent validator gaps: added `rejectionReason`, `selfReportedHarness`, `selfReportedModel`, and `customFormAnswers` to `baseStoryValidator`/`StoryWithDetailsPublic` so spread-based queries (listUserStories, listApprovedStoriesWithDetails, getStoryBySlug) don't fail return validation once those fields exist on a story. Existing groups unchanged (no config = all fields visible; old submissionFieldRequirements still honored). Verified: convex tsc clean, app tsc clean on touched files (remaining 100 errors pre-existing in other files), zero lints. PRD: prds/judging-group-custom-submit-form-fields.md. Files: convex/schema.ts, convex/judgingGroups.ts, convex/stories.ts, convex/judgingGroupSubmissions.ts, convex/validators.ts, src/components/admin/judging/GroupSubmitPageSection.tsx, src/components/admin/judging/groupSection.tsx, src/pages/JudgingGroupSubmitPage.tsx, src/pages/JudgingInterfacePage.tsx
- [x] 2026-08-09 10:25 UTC: Components check re-add fix and AI judge links hidden while disabled. (1) The Components check preset row in Rubric weights was an On/Off toggle pill; after deleting the criterion the pill returned in the Off state and clicking it silently re-added the criterion. Replaced with an explicit "Add to rubric" button labeled "preset, not in rubric" (backend was never re-adding; updateAiCustomCriteria clears correctly). (2) Reversed the same-day always-visible dimmed AI results row per user feedback: the Links ledger now lists the AI results page and agent API endpoints only while aiJudgeEnabled is true, the Agent API card explains when the AI judge is off, and the disabled-state rendering was removed from LinkEntry/LinkLedgerRow and the markdown export. Overview, edit modal, and the AI settings links block were already gated. Verified: app tsc clean on touched files, zero lints. PRD: prds/ai-judge-components-check-persistence-and-links-hide.md. Files: src/components/admin/judging/GroupAiSection.tsx, src/components/admin/judging/GroupLinksSection.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-09 10:05 UTC: AI judge links visibility. (Partially superseded by the 10:25 entry: the dimmed always-visible ledger row was reversed; the AI judge links block in the settings card stands.) The Links ledger now always lists the AI results page: dimmed with a gray lock and "Disabled" badge plus a pointer note when the AI judge is off, active with the normal password/public state when on (markdown export marks it Disabled too). New "AI judge links" block at the bottom of the AI judge settings card shows the AI results page and agent API endpoints (OpenAPI + base URL, hidden when the agent API toggle is off) with the same copy/open rows, rendered only after aiJudgeEnabled is saved server side. LinkLedgerRow/LinkEntry exported from GroupLinksSection for reuse. Dev data audit confirmed the query path was correct (groups with aiJudgeEnabled true exist); the fix is discoverability. Verified: app tsc clean, zero lints. PRD: prds/judging-group-ai-judge-links-visibility.md. Files: src/components/admin/judging/GroupLinksSection.tsx, src/components/admin/judging/GroupAiSection.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-09 09:55 UTC: Search-and-add submissions inside the judging group workspace. New "Add submissions" card at the top of the Submissions section: live title search across every site submission with per-row Add buttons, "In group" flags, and pending-status pills. Backed by new judgingGroupSubmissions.searchStoriesForGroup query (judging.manage permission so judging admins do not need moderation access; uses the stories search_all index, excludes hidden/archived/rejected, returns inGroup flag) and the existing addSubmissions mutation (dedupe + pending status). Added submissions flow into judge queues and AI judge runs automatically. AdminDocs manual add section rewritten. Verified: convex typecheck + app tsc clean, zero lints. PRD: prds/judging-group-manual-add-submissions.md. Files: convex/judgingGroupSubmissions.ts, src/components/admin/judging/GroupSubmissionsSection.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-09 09:30 UTC: Per-group scoring scale (1-5 or 1-10) plus criteria section cleanup. New optional `scoreScale` field on judgingGroups (unset = 10, no migration needed) with a Scoring scale selector in the workspace Settings section. Threaded through the whole stack: submitScore/updateJudgeScore/agent submissions validate 1..scale, all score queries return scoreScale and use it for maxPossibleScore, judge interface renders 1..scale buttons and /scale labels, results dashboards and JudgeTracking edit modal are scale-aware, agent API docs mention the per-group scale. AI judge keeps its own fixed 1-10 rubric. JudgingCriteriaEditor dropped its back button and page header (workspace sidebar handles navigation) and takes a scoreScale prop for its preview. Also fixed a pre-existing multi-judge bug: "Judged & Next" checked a nonexistent `thisJudgeCompleted` field, now uses `isComplete`. Judges-per-submission audited end to end (settings save, session/progress queries, completion threshold, score locks, agent locks, tracking banner) and works with the new layout. Verified: app tsc + convex typecheck clean, zero lints. PRD: prds/judging-score-scale-and-criteria-header.md. Files: convex/schema.ts, convex/judgingGroups.ts, convex/judges.ts, convex/judgeScores.ts, convex/judgingGroupSubmissions.ts, convex/adminJudgeTracking.ts, convex/agentJudges.ts, convex/http.ts, src/components/admin/judging/GroupSettingsSection.tsx, src/pages/JudgingInterfacePage.tsx, src/components/admin/JudgingCriteriaEditor.tsx, src/pages/AdminJudgingGroupPage.tsx, src/components/admin/JudgeTracking.tsx, src/components/PublicJudgingResultsDashboard.tsx, src/components/admin/JudgingResultsDashboard.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-09 07:14 UTC: Judging group workspace layout polish. Embedded Results, AI results, and Judge tracking sections no longer render their own back arrows or duplicate title headers (navigation lives in the workspace sidebar); JudgeTracking keeps its header only when used as the standalone page. Judging Results overview stat icons shrunk from w-8 h-8 blocks to w-3.5 h-3.5 inline with the labels for a compact strip. Workspace sidebar can now collapse to icon-only mode via a PanelLeft/PanelLeftClose toggle in the header (state persisted in localStorage, tooltips + aria labels on icons), expanding the content area; sidebar width tightened (md:w-44, md:w-10 collapsed) and workspace spacing reduced. AIJudgeResults keeps a groupName prop for the Stats panel subtitle. Verified: tsc clean on touched files, zero lints. Files: src/pages/AdminJudgingGroupPage.tsx, src/components/admin/JudgingResultsDashboard.tsx, src/components/admin/AIJudgeResults.tsx, src/components/admin/JudgeTracking.tsx
- [x] 2026-08-09 07:05 UTC: Copy all and markdown export on the links ledger. The Shareable links card header now has a "Copy all" button that copies every link as markdown (grouped Shareable links / Agent API, each with its access state: public, password protected with set status, or key required) and a ".md" button that downloads the same list as `slug-links.md`. Rows and the export render from one shared link-entry array so they never drift. AdminDocs Links ledger section updated. Verified: tsc clean, zero lints. PRD: prds/judging-groups-ai-prompt-criteria-links-api-toggle.md. Files: src/components/admin/judging/GroupLinksSection.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-09 06:55 UTC: Inline tag creation in the Required tag picker (Submit page section). The picker now uses tags.listAllForDropdown so hidden tracking tags are selectable (marked "hidden"), and typing a name with no exact match shows a "Create tag" row that calls the standard tags.create mutation (tags.manage permission, Tag Management slug convention, appears everywhere tags do). New tags default to hidden via a checkbox (recommended for tracking tags) and are auto-selected as the required tag. AdminDocs required tag sync section updated. Verified: tsc clean, zero lints. Files: src/components/admin/judging/GroupSubmitPageSection.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-09 06:50 UTC: Rubric weights per-criterion toggles and components check relocation. Every criterion row in the Rubric weights card (built-in and custom) now has an on/off toggle; off criteria are excluded from the AI prompt, scoring, and rankings on the next run (stored in new `aiDisabledCriteria` field, at least one criterion must stay on, weight input dims while off). The components check preset moved from the Custom criteria card into the Rubric weights card as a toggle row: switching it on adds the criterion immediately and it then behaves like any custom criterion. Stale disabled keys are pruned when custom criteria change. Verified: convex codegen + app tsc clean, zero lints. PRD: prds/judging-groups-ai-prompt-criteria-links-api-toggle.md. Files: convex/schema.ts, convex/aiJudge.ts, convex/judgingGroups.ts, src/components/admin/judging/GroupAiSection.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-09 06:35 UTC: Judging group AI and links upgrades. Per group: admin-defined custom AI criteria (up to 10, with a one-click "components check" preset that scores repo-verified Convex component usage) appended to the built-in rubric with their own weights; editable AI judge system prompt (view, edit, paste, reset to default; `{{rubric}}` placeholder, JSON response contract always enforced server side); agent HTTP API on/off toggle (403 on all keyed calls and key creation blocked while off, keys kept for re-enable); new Links sidebar section as a realtime ledger of every shareable URL with lock icons and live password-set status plus agent API endpoints. AI judge context expanded: reads repo-root hackathon log files (hackathon.md, changelog.md, task.md, files.md, 5k chars each), lists agent skills in the repo, and fetches a published /hackathon.json manifest from the live app origin as the fallback for private/no-repo teams (self-reported sources never override verified facts). AdminDocs expanded with a workspace sections/permissions table, links ledger, access gates per URL, and all new AI options. Verified: convex codegen typecheck clean, app tsc clean for touched files, zero lints. PRD: prds/judging-groups-ai-prompt-criteria-links-api-toggle.md. Files: convex/schema.ts, convex/aiJudge.ts, convex/aiJudgeAnalysis.ts, convex/agentJudges.ts, convex/http.ts, convex/judgingGroups.ts, src/components/admin/judging/GroupAiSection.tsx, src/components/admin/judging/GroupLinksSection.tsx (new), src/pages/AdminJudgingGroupPage.tsx, src/components/admin/AdminDocs.tsx
- [x] 2026-08-08 19:55 UTC: Audited the entire admin and judging backend for more null-into-patch bugs like the updateGroup one. Reviewed every mutation with nullable args (tags.create/update, convexBoxConfig.update, users.syncUserFromClerkWebhook, judgingGroups.updateGroup) and every dynamic/spread patch (forms, submitForms, storyFormFields, settings, tags.updateTagLimits, all judging files); frontend judging sections only send null to args that accept null; schema has zero stored-null fields. Result: updateGroup was the only offender and is already fixed; no other changes required. PRD: prds/submit-page-enable-404-and-header-save.md
- [x] 2026-08-08 19:45 UTC: Fixed Submit page section save failing with "Value does not match validator" on `submissionFormRequiredTagId: null`. The `updateGroup` generic copy loop passed null through to `ctx.db.patch` for nullable-to-clear fields (required tag, page/form titles, description, image id) while only passwords, dates, and auto-include fields converted null to undefined; `v.optional` schema fields reject null so the whole save aborted (layout change from two-column to single never applied). Copy loop now maps null to undefined for all fields. Verified: convex dev push clean at 12:43, no lint errors. PRD: prds/submit-page-enable-404-and-header-save.md. Files: convex/judgingGroups.ts
- [x] 2026-08-08 19:40 UTC: Fixed custom submission page 404 after enabling. The Enabled toggle in GroupSubmitPageSection was local state only; the card showed the live URL immediately but `hasCustomSubmissionPage` was not written until the footer Save far below, so `getSubmissionPage` returned null and the public page 404ed. The toggle now persists immediately through `updateGroup` (rollback + inline error on failure), matching the Overview status toggles. Also added a `headerAction` slot to SectionCard plus a compact HeaderSaveButton rendered at the top of the two long cards (Custom submission page, AI judge) when expanded, so a save is always visible without scrolling; footer saves unchanged. AI enable toggle intentionally stays save-gated because its save validates the private-results password. Verified: no lint errors on touched files. PRD: prds/submit-page-enable-404-and-header-save.md. Files: src/components/admin/judging/groupSection.tsx, GroupSubmitPageSection.tsx, GroupAiSection.tsx
- [x] 2026-08-08 19:05 UTC: Custom submission page single column layout. Added a third admin-selectable layout ("single") for judging group submission pages: centered hero (square header image capped at the configured size, title, description, page links as pill buttons) stacked above the form card in a max-w-2xl column. Widened the `submissionPageLayout` union in schema and all three judgingGroups validators; added the option to both the GroupSubmitPageSection picker and the legacy EditJudgingGroupModal select. Form polish across all layouts: hairline section headings (Project details / Links and media / About you / Tags), space-y-5 rhythm, Selected Tags summary in a muted card, taller submit button; form card extracted so all layouts share identical behavior. Also fixed the readonly mapped-type error in resolveRequirements. Verified: tsc clean for touched files, no lint errors, convex dev push clean. PRD: prds/submission-page-single-column.md. Files: convex/schema.ts, convex/judgingGroups.ts, src/components/admin/judging/GroupSubmitPageSection.tsx, src/components/admin/EditJudgingGroupModal.tsx, src/pages/JudgingGroupSubmitPage.tsx
- [x] 2026-08-08 09:00 UTC: Admin judging UI redesign. Judging tab is now a compact list of groups (status dot, lock icon, sub/judge counts, created time) with an inline totals line and Create button; each row links to a new full-page docs-style workspace at `/admin/judging/:slug` with a sticky sidebar and `?section=` deep links (Overview, Settings, Access, Criteria, Submissions, Submit page, AI judge, Results, AI results, Judge tracking). The 2,000-line EditJudgingGroupModal was decomposed into independent section panels that each save via the existing `updateGroup` partial-update mutation (password blank-keeps-existing behavior preserved); Criteria/Results/AI results/Tracking embed the existing components unchanged. Sidebar sections are gated by delegated permissions (judging.manage/results/tracking/ai/delete). Also tightened AdminDashboard spacing (page padding, header, 13px tabs). Verified: tsc clean for all touched files (remaining errors pre-existing), no linter errors. PRD: prds/admin-judging-ui-redesign.md. Files: src/pages/AdminJudgingGroupPage.tsx (new), src/components/admin/judging/groupSection.tsx + GroupOverviewSection/GroupSettingsSection/GroupAccessSection/GroupSubmissionsSection/GroupSubmitPageSection/GroupAiSection (new), src/components/admin/Judging.tsx, src/components/admin/AdminDashboard.tsx, src/App.tsx
- [x] 2026-08-08 08:30 UTC: Tag limits and bulk tag cleanup. Added settings fields `maxTagsPerSubmission` (default 6) and `maxTagLength` (default 20) with a "Tag limits" card in Tag Management (saved via new `tags.updateTagLimits`, gated on tags.manage). Server-side enforcement in `stories.submit`, `submitAnonymous`, and `updateOwnStory`: visible tag count capped at the setting (hidden tags exempt so custom-form tracking tags like resendhackathon/ychackathon always work), new tag names over the length limit rejected unless they match an existing tag. All submit forms (StoryForm, JudgingGroupSubmitPage, ResendForm, YCHackForm) now use the settings-based limit instead of hardcoded 10, cap the new-tag input with maxLength, and show count/max. Bulk tag cleanup in Tag Management: per-row checkboxes, select-all-on-page, action bar with Archive/Unarchive/Delete (inline two-step delete confirm) backed by new `tags.bulkSetHidden` and `tags.bulkDeleteTags` mutations. Verified: convex dev push clean, no lint errors, frontend tsc shows only pre-existing errors. PRD: prds/tag-limits-and-bulk-cleanup.md. Files: convex/schema.ts, convex/settings.ts, convex/tags.ts, convex/stories.ts, src/components/admin/TagManagement.tsx, src/components/StoryForm.tsx, src/pages/JudgingGroupSubmitPage.tsx, src/components/ResendForm.tsx, src/components/YCHackForm.tsx
- [x] 2026-08-08 06:55 UTC: Access tab judging group picker upgrade. Added a search box (filters by group name or slug, client-side over the already-loaded `listGroupsForAccess` data), removable selected-group chips above the list, active-first alphabetical sort, and a selected count. Files: `src/components/admin/AccessManagement.tsx`. Verified: tsc clean for the touched file, no linter errors.
- [x] 2026-08-07 16:20 UTC: Admin access permissions, judging delegation, and in-admin docs. New `adminPermissions` Convex table (by_userId, by_clerkId) plus `convex/adminAccess.ts` with `getAccessContext`, `hasPermission`, `requirePermission`, `requireJudgingGroupPermission`, `getAllowedJudgingGroupIds`, `getMyAdminAccess`, and full-admin-only grant/revoke/list/search CRUD. Swapped `requireAdminRole` for granular permission keys across tags, stories, comments, forms, storyFormFields, submitForms, users, reports, settings, emails, adminQueries, convexBoxConfig, and all judging files with group-scoped checks (list queries filter to allowed groups; entity mutations resolve the row's groupId before checking). Frontend: `useAdminAccess` hook + provider, AdminDashboard renders only permitted tabs with a Delegated access badge, new Access tab (user search, section permission cards with action toggles and destructive highlighting, judging group multi-select or all groups, summary chips, notes, grants list with edit/revoke confirm), new Docs tab (sidebar-nav markdown docs for groups, criteria, submissions, judge flow, results/tracking, AI judge with verified GitHub reading behavior and env vars, agent judges HTTP API, delegated access), Judging list hides ungrated actions, and JudgeTrackingPage/FormBuilder/FormResults gates use the access query. Verified: convex codegen + TS clean, eslint 0 errors, prod build passes, adminPermissions table live on dev. PRD: prds/admin-access-permissions.md. Files: convex/schema.ts, convex/adminAccess.ts, convex/users.ts, convex/tags.ts, convex/stories.ts, convex/comments.ts, convex/forms.ts, convex/storyFormFields.ts, convex/submitForms.ts, convex/settings.ts, convex/reports.ts, convex/adminQueries.ts, convex/sendEmails.ts, convex/emails/broadcast.ts, convex/convexBoxConfig.ts, convex/judgingGroups.ts, convex/judgingCriteria.ts, convex/judges.ts, convex/judgingGroupSubmissions.ts, convex/judgeScores.ts, convex/adminJudgeTracking.ts, convex/aiJudge.ts, convex/agentJudges.ts, src/components/admin/useAdminAccess.tsx, src/components/admin/AccessManagement.tsx, src/components/admin/AdminDocs.tsx, src/components/admin/AdminDashboard.tsx, src/components/admin/Judging.tsx, src/components/admin/FormBuilder.tsx, src/components/admin/FormResults.tsx, src/pages/JudgeTrackingPage.tsx
- [x] 2026-07-05 22:11 UTC: Agent Ready component installed. Added @waynesutton/agent-ready plus @convex-dev/crons and @convex-dev/workpool to convex.config.ts, registered its HTTP routes in convex/http.ts (llms.txt and robots.txt skipped, app serves those from siteFiles), added admin-gated wrappers convex/agentReady/content.ts and analytics.ts, and agent-ready.config.json. Files: convex/convex.config.ts, convex/http.ts, convex/agentReady/, agent-ready.config.json, package.json
- [x] 2026-07-05 21:55 UTC: AI Judge Convex components detection and scoring. Repo fetch now extracts components from package.json (`@convex-dev/*` deps, eslint plugin excluded) and convex.config.ts imports of `*/convex.config` (local components included), stored as `componentsDetected` on aiJudgeResults. Prompt updates reward component usage: advanced criterion should generally score 7+ when components are used well, components named in reasoning and listed as `component: <name>` in features. Stats tab adds a "Using Convex components" card and a "Components used" list with per-app counts; hackathon report adds an overview components row and per-submission components line. Old results fall back to feature keyword for the apps count; re-run reviews to populate the list. Verified: convex codegen + tsc clean, frontend tsc shows only pre-existing errors, no lint errors. PRD: prds/ai-judge-components.md. Files: convex/schema.ts, convex/aiJudge.ts, convex/aiJudgeAnalysis.ts, src/components/admin/AIJudgeResults.tsx
- [x] 2026-07-05 21:35 UTC: Fixed production server errors on `tags:getBySlug` and `stories:getBySlug` after hiding a tag. The per-view tag visibility fields (`hideInStoryDetail`, `hideInStoryList`) added on 2026-06-28 were missing from the `getBySlug` return validator and the shared `tagDocValidator`, so any tag document carrying those fields failed Convex return validation and crashed tag pages and app detail pages. Added both optional fields to the validators and the matching `StoryWithDetailsPublic` type. Verified: tsc clean, no lint errors, convex dev functions ready. Files: convex/tags.ts, convex/validators.ts
- [x] 2026-07-05 20:25 UTC: AI Judge stats tab + hackathon report. Added a Results/Stats/Hackathon Report tab bar to the admin AI Review view. Stats tab (enabled once one review completes) is a screenshot-friendly rollup: apps reviewed, apps using Convex, apps using advanced Convex features (keyword match or advanced criterion >= 6), live apps, repos analyzed, average score, top detected features with bars, and a score distribution chart. Hackathon Report tab is grayed out until pending = 0 and running = 0, then generates a markdown report client-side (overview table, participation, team names + members, rankings table with GitHub/live app links, per-submission details with scores and AI notes, failed reviews section) with view, Copy Markdown, and Download .md. New admin-only `getGroupAiReportData` query keeps team emails out of public queries. Verified: convex codegen + tsc clean, no lint errors. PRD: prds/ai-judge-stats-report.md. Files: convex/aiJudge.ts, src/components/admin/AIJudgeResults.tsx
- [x] 2026-07-05 19:55 UTC: AI Judge live app URL check. Each AI review now makes a direct HTTP request to the submission's live app URL (never social links) and records `urlCheck` (isLive, statusCode, note) on the result row. Added a sixth "Live app status" rubric criterion; a dead/404 URL is clamped server-side to max 2 (3 when no URL provided) so the model can't overrule the observed status, the broken status is appended to the overall note, and the other five Convex criteria stay untouched so ranking remains mostly Convex-focused. Firecrawl scrape success counts as live for crawler-friendly hosts that block plain GETs. Admin and public results show URL live / URL 404 / URL down / no URL badges; old results without the field render unchanged. Verified: convex codegen + tsc clean, no lint errors in touched files. PRD: prds/ai-judge-url-liveness.md. Files: convex/schema.ts, convex/aiJudge.ts, convex/aiJudgeAnalysis.ts, src/components/admin/AIJudgeResults.tsx, src/pages/AIJudgeResultsPage.tsx
- [x] 2026-07-05 18:00 UTC: AI Judge (Best Use of Convex) for judging groups. Opt-in per group: fixed Convex rubric, GitHub + Firecrawl context, Anthropic → OpenAI → OpenRouter fallback, sequential scheduler chain, admin results view with inline editing and retry, public password-gated results page at /judging/:slug/ai-results. No author notifications, no changes to human judging. Files: convex/schema.ts, convex/judgingGroups.ts, convex/aiJudge.ts, convex/aiJudgeAnalysis.ts, src/components/admin/CreateJudgingGroupModal.tsx, src/components/admin/EditJudgingGroupModal.tsx, src/components/admin/AIJudgeResults.tsx, src/components/admin/Judging.tsx, src/pages/AIJudgeResultsPage.tsx, src/App.tsx
- [x] 2026-06-30 18:17 UTC: Judging auto-jump to next submission. In the single-judge flow, `handleMarkCompleted` now advances to the next submission after a successful "Mark Submission Complete", matching the multi-judge "Judged & Next" behavior. Prefers the next submission not completed by any judge (via `judgeProgress.submissionProgress[].completedBy`), otherwise moves forward by one. Verified: lint clean on changed file. Files: src/pages/JudgingInterfacePage.tsx
- [x] 2026-06-30 17:40 UTC: Judging auto-include UX upgrade.  Made the auto-populate tag picker searchable (filter by name, first 50 results) with removable selected-tag chips that persist when filtered out, so admins can handle 1000s of tags. Added a "Tag match rule" (any/all): "any" keeps OR behavior, "all" requires every selected tag (AND, e.g. tag 1 + tag 2 + tag 3). New optional `autoIncludeMatchMode` field (defaults "any"); `storyMatchesAutoInclude`, `updateGroup` backfill, validators, and `getGroupWithDetails` all honor it. Date range still applies in both modes. Verified: convex codegen + deploy TypeScript clean, frontend lints clean. PRD: prds/judging-tag-daterange-groups.md. Files: convex/schema.ts, convex/judgingGroups.ts, convex/judgingGroupSubmissions.ts, src/components/admin/EditJudgingGroupModal.tsx
- [x] 2026-06-29 17:14 UTC: Modal Escape-to-close audit. Audited every modal/overlay and ensured Escape closes the open one. Added a shared `src/hooks/useEscapeKey.ts` (window keydown subscription gated on open state) and wired it into the shared `ui/dialog.tsx` (covers Create/Edit submit form modals, StoryDetail report, UserProfilePage report), `AlertDialog`, EditJudgingGroupModal, CreateJudgingGroupModal, ContentModeration delete-comment confirm, UserModeration confirm action, JudgeTracking (edit score / delete judge / delete score), and InboxPage (block / report). Left already-working modals untouched (MessageDialog, PromptDialog, ImageGallery, StoryForm lightbox, Radix AuthRequiredDialog + Footer about). No UI/behavior changes beyond ESC. Verified: `tsc --noEmit` exit 0, eslint 0 errors on changed files. PRD: prds/modal-esc-close.md. Files: src/hooks/useEscapeKey.ts, src/components/ui/dialog.tsx, src/components/ui/AlertDialog.tsx, src/components/admin/EditJudgingGroupModal.tsx, src/components/admin/CreateJudgingGroupModal.tsx, src/components/admin/ContentModeration.tsx, src/components/admin/UserModeration.tsx, src/components/admin/JudgeTracking.tsx, src/pages/InboxPage.tsx
- [x] 2026-06-30 00:17 UTC: Permanently fixed Convex write conflicts in the `judges` table. The `judges.updateActivity` heartbeat was retrying ~1.6K times on a single hot row (conflicting with itself) because it wrote `lastActiveAt` on essentially every 60s tick, so parallel heartbeats collided under OCC (https://docs.convex.dev/error#1). Fix: gated the write behind a 2-minute server-side staleness threshold (`ACTIVITY_UPDATE_THRESHOLD_MS`) so most heartbeats are read-only no-ops and concurrent calls resolve to one write; added client jitter (60s + up to 15s) so heartbeats don't fire in lockstep. Created `.agents/skills/convex-write-conflicts/SKILL.md` to remember the diagnosis + canonical fix. Verified: lint clean on both files. Files: convex/judges.ts, src/pages/JudgingInterfacePage.tsx, .agents/skills/convex-write-conflicts/SKILL.md
- [x] 2026-06-30 00:11 UTC: Judging groups auto-populate by multiple tags + date range. Admins can configure a group with multiple tags (OR logic, one or both) and an optional submission creation date range; matching stories are materialized into `judgingGroupSubmissions` (idempotent via `ensureStoryInGroup`) so they appear in JudgingInterfacePage and judging results with no extra wiring. Added `autoIncludeTagIds`/`autoIncludeStartDate`/`autoIncludeEndDate` schema fields, `storyMatchesAutoInclude` helper, extended `syncStoryToTaggedGroups`, new `syncAutoIncludeSubmissions` mutation, forward backfill in `updateGroup`, and a multi-tag + date range + "Sync matching submissions" UI section. Filters only add submissions, never remove (preserves scores). Verified: convex codegen + deploy typecheck clean, frontend lints clean. PRD: prds/judging-tag-daterange-groups.md. Files: convex/schema.ts, convex/judgingGroups.ts, convex/judgingGroupSubmissions.ts, src/components/admin/EditJudgingGroupModal.tsx
- [x] 2026-06-28 23:10 UTC: Judging group CSV export. Each judging group in the Admin Judging System now has an "Export CSV" button that fetches the group's submissions on demand (admin-only `exportGroupSubmissions` query) and downloads custom submit form info: App Title, App/Project Tagline, Description, App Website Link, Video Demo URL, GitHub/LinkedIn/Twitter/Chef links, Tags, Hackathon Team Info, submitter name, email, slug, votes. Images excluded; hidden/archived/rejected stories skipped; empty groups show a notification. PRD: prds/judging-group-csv-export.md. Files: convex/judgingGroupSubmissions.ts, src/components/admin/Judging.tsx
- [x] 2026-06-28 23:00 UTC: Per-view tag visibility. Admins can hide a tag from the app detail page and from app card lists (home/category/grid/vibe) independently, alongside existing header and archive toggles, so custom submission tags can be hidden from public surfaces without hardcoding names. Added optional `hideInStoryDetail`/`hideInStoryList` tag fields, surfaced two new toggle buttons in Tag Management, threaded the fields through story tag resolution, and applied the filters in StoryDetail and StoryList. PRD: prds/tag-per-view-visibility.md. Files: convex/schema.ts, convex/tags.ts, convex/stories.ts, src/components/admin/TagManagement.tsx, src/components/StoryDetail.tsx, src/components/StoryList.tsx
- [x] 2026-06-28 22:48 UTC: Judge score breakdown on results page. Added collapsible per-judge name and average toggle to Rankings on the public judging results dashboard for multi-judge submissions. Backend returns judgeBreakdown when judgesCompleted > 1. Files: convex/judgeScores.ts, src/components/PublicJudgingResultsDashboard.tsx
- [x] 2026-06-28 22:40 UTC: Judging submissions counted by required tag. Any valid story carrying a group's required tag is now materialized into `judgingGroupSubmissions` + `submissionStatuses` so it is judged and counted like a custom-form submission. Reverse sync on story tag edits (`updateOwnStory`, `updateStoryAdmin`, `addTagsToStory`), forward backfill when a group's required tag is set/changed (`updateGroup`), and a manual "Sync existing submissions with this tag" admin button. Idempotent via shared `ensureStoryInGroup` helper; preserves existing scores/statuses. PRD: prds/judging-required-tag-submissions.md. Files: convex/judgingGroupSubmissions.ts, convex/stories.ts, convex/judgingGroups.ts, src/components/admin/EditJudgingGroupModal.tsx
- [x] 2026-06-28 22:17 UTC: Multi-judge submissions: admins can set judges per submission (1-20), "Judged & Next" flow with per-judge completion tracking (new submissionJudgeCompletions table), overall + per-judge score breakdown with after-self reveal, OCC-safe completion. PRD: prds/multi-judge-submissions.md. Files: convex/schema.ts, convex/judgingGroups.ts, convex/judges.ts, convex/judgingGroupSubmissions.ts, convex/judgeScores.ts, convex/adminJudgeTracking.ts, src/components/admin/EditJudgingGroupModal.tsx, src/pages/JudgingInterfacePage.tsx, src/components/admin/JudgeTracking.tsx
- [x] 2026-06-28 14:33 UTC: Installed agent skill scaffolding into the repo: `.agents/skills/*` and `.claude/skills/*` (convex-quickstart, convex-setup-auth, convex-create-component, convex-migration-helper, convex-performance-audit, plus design/workflow skills), with `AGENTS.md`, `CLAUDE.md`, and `skills-lock.json`. Dev tooling only, no app code or schema changes. Verified production build passes (`npm run build`, exit 0). Files: `.agents/skills/`, `.claude/skills/`, `AGENTS.md`, `CLAUDE.md`, `skills-lock.json`
- [x] 2026-06-28 07:00 UTC: Tag Management top pagination. Added a second pagination control next to the "Tags per page" selector at the top of the list; kept the existing bottom control. Both render from one shared `renderPagination()` helper driven by the same `currentPage`/`safePage`/`totalPages` state, so they always stay in sync. Files: `src/components/admin/TagManagement.tsx`
- [x] 2026-06-28 06:45 UTC: Lint cleanup to 0 ESLint errors. Fixed 4 pre-existing `react-hooks/rules-of-hooks` violations (moved early returns below hooks in MessageDialog, ImageGallery, ContentModeration); fixed no-useless-escape, no-case-declarations, and empty-interface errors; set `no-explicit-any` and `no-unused-vars` to `warn` (with ignoreRestSiblings + `^_`) so 400+ pre-existing items stay advisory. Verified: 0 lint errors, `tsc` clean, prod build OK, convex functions ready, behavior unchanged. Files: `eslint.config.js`, `convex/clerk.ts`, `convex/emails/templates.ts`, `netlify/edge-functions/submitMeta.ts`, `src/components/ui/MessageDialog.tsx`, `src/components/ImageGallery.tsx`, `src/components/admin/ContentModeration.tsx`, `src/components/ResendForm.tsx`, `src/components/StoryForm.tsx`, `src/components/YCHackForm.tsx`, `src/components/StoryDetail.tsx`, `src/pages/JudgingInterfacePage.tsx`
- [x] 2026-06-28 06:25 UTC: Upgraded Convex to 1.42.0 (latest) + gated components (`@convex-dev/resend` 0.2.5, `rate-limiter` 0.3.2, `workpool` 0.4.7, `convex-helpers` 0.1.120); added `@convex-dev/eslint-plugin@2.0.0` and bumped `typescript-eslint` to fix the eslint 9.36 crash; cleaned `convex/sendEmails.ts` unused bindings and ran autofix. Verified: convex dev ready, vite dev clean, `tsc --noEmit` clean, `convex codegen` typecheck clean, prod build OK. Files: `package.json`, `package-lock.json`, `eslint.config.js`, `convex/sendEmails.ts`
- [x] 2026-06-27: Fixed global email toggle so Disabled blocks all email types (admin report notifications no longer bypass the kill switch); added tag-based broadcast emails (send to everyone who used a tag) with a searchable tag selector, submission-status filter (pending/approved/rejected), and live recipient count in Email Management. Files: `convex/emails/resend.ts`, `convex/emails/broadcast.ts`, `src/components/admin/EmailManagement.tsx`
- [x] 2026-06-28 06:15 UTC: Tag Management performance + UX. Save now persists only changed tags (was re-saving all 400+); drag-and-drop reorder persists only the moved range in parallel; added pagination with selectable page size (5-200) and search across all tags. Files: `src/components/admin/TagManagement.tsx`
- [x] 2026-06-28 02:52 UTC: Judging custom submission page admin-selectable required fields (PRD: prds/judging-custom-required-fields.md)
- [x] updated bookmarks viewing
- [x] changed site fonts
- [x] change from /u/username to /username in url slugs
- [x] Add a ban member in /admin dashboard
- [x] add sidebar leaderboard to list view
- [x] update clerk admin role permissions in for mod view
- [x] create 404 page
- [x] remove state for profile and admin page to remove sidebar
- [x] add tag bar to header like https://www.ycombinator.com/launches
- [x] add follow profile features
- [x] update comments to have a 10 word minimum
- [x] fix username and name clerk sync
- [x] hide or show views in the dashboard
- [x] add show 3 related products by tags below comments
- [x] update category links in vibe view sidebar
- [x] add rabbit analytics https://app.rybbit.io/
- [x] Joined vibe apps date
- [x] setup clerk production and link
- [x] link to sections on profile page
- [x] download icons for all the apps
- [x] sort tags in dashboard
- [x] Setup hosting on Netlify
- [x] Setup domain name on Netlify
- [x] confirm https://www.cookieyes.com/ is setup
- [x] test forms
- [x] setup llms.txt, sitemap.xml and robots.txt
- [x] link image in setting popup box
- [x] fix name on story detailed page
- [x] hide resendhackathon on story detailed page
- [x] hide extra sign in to commit
- [x] add new tags url slugs for pages
- [x] put reports under user moderation in admin dashboard
- [x] judging scale from 1-10 vs 1-5
- [x] search for tags in admin
- [x] allow to number sort tags
- [x] search for submission name next to number in the judging form
- [x] add view judging results to the judging form
- [x] Add options to add team
- [x] add option for multiple image upload
- [x] added markdown support and preview for story details page
- [x] fix open graph image
- [x] add gallery images also show up in judging page with popup modal
- [x] added judges score moderations system
- [x] add @mentions for usernames in judging notes, comments for profiles and in the comments section.
- [x] add notification section like product hunt that works with sending emails too, move notification dot and background from page
- [x] update templates.ts
- [x] fix links for weekly emails
- [x] report a user option on profile page
- [x] update alert's if admin pins or adds a message to their app
- [x] setup resend for emails for notifications on post
- [x] add option to turn off email notifications on user profiles
- [x] add reporting users or post email option to admin from adminalerrtemails.md
- [x] add changelog tracking on story detail page that shows all user edits with date/time in user's timezone
- [x] always show changelog section with anchor links from sidebar and judging page
- [x] add inbox direct message and inbox email notification from friendsonlyinbox.md
- [x] test inbox direct message and inbox email notification from friendsonlyinbox.md
- [x] change notifications for inbox to be inbox check in
- [x] fix "Invalid Date" display on judging interface page submission timestamps
- [x] add inbox icon to header
- [x] fix inbox scrolling behavior to prevent whole page from scrolling, only chat window and conversation list scroll independently
- [x] redesign inbox page with messenger-style 3-column layout with right sidebar showing Most Vibes This Week, Recent Vibers, and Top Categories This Week
- [x] implement soft delete for inbox conversations with automatic undelete when receiving new messages
- [x] add clickable @username links in inbox chat messages that link to user profiles (without hover cards)
- [x] make usernames in conversation list and chat header clickable links to user profiles
- [x] fix "Not authenticated" errors in markConversationRead mutation
- [x] fix deleted conversations showing old messages when restarted - now marks all messages as deleted when deleting conversation
- [x] fix deleted conversation still appearing in inbox - added auto-clear when conversation no longer exists in list
- [x] fix missing input box when starting new conversation after deletion - added fallback query to fetch conversation details
- [x] fix upsertConversation to remove current user's deletion record when restarting conversation
- [x] fix message sync - when user deletes conversation and other user sends message, conversation reappears in recipient's inbox
- [x] add report user feature in inbox conversation header with Flag icon (light grey, hover black)
- [x] integrate inbox user reports with UserReportManagement admin dashboard
- [x] send email alerts to admins/managers when users are reported in inbox
- [x] add block user feature in inbox conversation header with Ban icon (light grey, hover black)
- [x] blocked users cannot send messages to users who blocked them
- [x] block button shows block/unblock state and allows toggling
- [x] replace browser alerts/prompts with custom modals matching site design
- [x] add error banner for blocked user messages instead of console errors
- [x] add custom report modal with textarea (500 char limit)
- [x] in judging interface keep the original date but ia user make changes add just the date and time the submission was modified and still link to change log
- [x] add an anchor link to change log and link to
- [x] in Content moderation add a show by date range submissions
- [x] Content moderation keep hide, add archive that hides from Content moderation but still able to see by dropdown selection
- [x] fixed judging progress
- [x] added edit add tags to submissions
- [x] added CSV export to Judge Tracking dashboard for comprehensive judge activity reports
- [x] updated Judge Tracking UI to show submissions judged and avg score instead of confusing scores column
- [x] fixed Judge Tracking crash when encountering deleted judges, stories, or criteria
- [x] added judge notes viewing and moderation in Judge Tracking dashboard
- [x] admins can view all judge collaboration notes on submissions and reply to them
- [x] added note count badges showing number of notes per submission in Judge Tracking
- [x] improved reply button design for better mobile responsiveness in judge notes
- [x] added total notes count per judge in judge list next to submissions judged stat
- [x] verified notes counts back-fill all existing/historical notes from database
- [x] improved Judge Tracking UI spacing and layout for better readability
- [x] enhanced CSV export to include judge collaboration notes with timestamps
- [x] added blank rows between submissions in CSV export for better organization
- [x] fixed Judge Tracking server error when scores reference deleted stories or criteria - now gracefully skips orphaned scores
- [x] fixed write conflicts in judges table by optimizing updateActivity and registerJudge mutations
- [x] reduced database queries in registerJudge from multiple filtered queries to single collect with in-memory filtering
- [x] added client-side throttling to updateActivity (60 seconds) and conditional updates (only if timestamp is newer)
- [x] added 1-minute threshold for lastActiveAt updates in registerJudge to prevent excessive writes
- [x] add dates to messages inbox
- [x] judge tracking now has its own dedicated page with URL pattern /admin/judging/{slug}/tracking for each judging group
- [x] made judge notes display with sticky note yellow background and black text for better visibility
- [x] inbox message from user on the right
- [x] rankings score don't show until judge has clicked on complete
- [x] add link to submission on judges score page
- [x] add inbox messages to daily engagement emails with sender name and count
- [x] fix critical date range bug causing incorrect email activity calculations
- [x] enhance email testing panel with date range display and activity warnings
- [x] move TESTING_SUMMARY.md and EMAIL_DATE_RANGE_FIX.md to prds folder
- [x] update addresend.md with Phase 11 implementation details
- [x] update adminalerrtemails.md to reference recent email infrastructure improvements and testing documentation
- [x] update email-testing-guide.MD with Phase 11 enhancements and cross-references to new testing documentation
- [x] update mentions.md to reference Phase 11 inbox message integration in daily email system
- [x] fix judging count for submission
- [x] enhanced multi-judge submission visibility to show ALL submissions to all judges for better transparency
- [x] added canEdit and completedBy fields to judge progress tracking in backend
- [x] removed submission filtering logic from frontend judging interface
- [x] updated progress counter to show group-wide completion (submissions completed by ANY judge)
- [x] enhanced search dropdown to show completion status and judge names
- [x] added UI controls to disable scoring for submissions completed by other judges
- [x] improved transparency and coordination in multi-judge scenarios
- [x] updated judgingsetup.md with comprehensive documentation of new multi-judge visibility system
- [x] updated changelog.MD with October 12, 2025 multi-judge visibility enhancement
- [x] moved Judge Scores & Comments section from PublicJudgingResultsDashboard to admin JudgeTracking page
- [x] reorganized JudgeTracking page layout: Stats Overview, Judge Scores & Comments, Judge Activity
- [x] simplified public results page to focus on rankings and criteria performance only
- [x] enhanced admin workflow with centralized judging data view in Judge Tracking
- [x] reorganized JudgeTracking page section order to Stats Overview → Judge Activity → Judge Scores & Comments
- [x] added breadcrumb navigation links to quickly jump between sections on JudgeTracking page
- [x] added floating scroll-to-top and scroll-to-bottom buttons on JudgeTracking page
- [x] added anchor IDs and smooth scroll behavior to all major sections for better navigation
- [x] updated JudgingInterfacePage progress bars to show group-wide completion percentage
- [x] changed progress calculation from individual judge completion to group-wide completion
- [x] synchronized all progress indicators (header bar, overall progress text, progress summary) to use groupCompletionPercentage
- [x] add emoji support in inbox
- [x] added EditJudgingGroupModal component for comprehensive judging group settings management
- [x] added tag filter and judged status filter to judging interface page
- [x] removed console.log statements from codebase for production readiness
- [x] fixed linter errors (unused imports and variables)

## In Progress Tasks

- [ ] Security review remaining items (deferred): ungated generateUploadUrl (16), dmReactions participant check (18), public form results toggle (19), unsubscribe token CSPRNG (20), fixMissingEmails/getCriterion (22), React Router 7. PRD: prds/security-review-2026-08-13.md.

## Future Tasks - Roadmap

- [ ] clerk roles for hackathon organizers to see judges section in admin dashboard only from friendsonlyinbox.md
- [ ] add admin moderation to make app post by approval only

## Bugs

## Implementation Plan

Detailed description of how the feature will be implemented.

### Relevant Files

- search [files.MD](files.MD)
